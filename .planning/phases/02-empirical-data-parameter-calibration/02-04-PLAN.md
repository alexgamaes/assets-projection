---
phase: 02-empirical-data-parameter-calibration
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/core/__tests__/sourcing.test.ts
  - src/core/__tests__/calibration.test.ts
  - src/core/__tests__/invariants.test.ts
  - src/data/defaults.ts
autonomous: true
requirements: [DATA-03, DATA-04]
must_haves:
  truths:
    - "The build/test pipeline fails if any shipped DEFAULTS parameter lacks a complete (non-empty) SourceRecord field"
    - "D-07: The frozen dragStrength is back-solved from a historical baseline run (not an asserted constant) and the test reproduces the back-solved value"
    - "D-08: The back-solve target is the McKinsey ~80% asset-inflation share of net-worth growth (NOT the ~1.3x asset/GDP ratio)"
    - "D-09: Running the engine on the real defaults at the back-solved dragStrength re-asserts the Phase 1 D-12 non-conservation / infinite-growth invariants (no finite-pie artifact)"
    - "No tier exceeds a documented plausible bound within the default horizon on the real defaults"
  artifacts:
    - path: "src/core/__tests__/sourcing.test.ts"
      provides: "D-11 completeness enforcement test + negative blank-field test"
      contains: "complete SourceRecord"
    - path: "src/core/__tests__/calibration.test.ts"
      provides: "Task 2 drag back-solve harness + frozen-value reproduction; Task 3 divergence sanity-check (same file, separate describe blocks)"
      contains: "asset-inflation share"
  key_links:
    - from: "src/core/__tests__/sourcing.test.ts"
      to: "src/data/defaults.ts DEFAULTS"
      via: "enumeration of every SourcedParam"
      pattern: "DEFAULTS"
    - from: "src/core/__tests__/calibration.test.ts"
      to: "src/data/defaults.ts dragStrength"
      via: "bisect back-solve reproduces McKinsey ~80%"
      pattern: "0.80"
---

<objective>
Add the enforcement and calibration layer on the real defaults: (1) the D-11 sourcing-completeness Vitest gate (DATA-04) that fails the build if any shipped `DEFAULTS` value lacks a complete `SourceRecord`, with a negative "test-the-test"; (2) the dragStrength back-solve harness (D-07: back-solve from a historical baseline run; D-08: target = McKinsey ~80% asset-inflation share, NOT the ~1.3x asset/GDP ratio) that derives dragStrength so the engine's asset-inflation share of net-worth growth over a 2000-2021-like baseline matches McKinsey ~80%, freezes the resulting value in `defaults.ts`, and asserts the frozen value reproduces the target and equals the back-solve; (3) the divergence sanity-check (success criterion 5); (4) the D-09 re-run of the Phase 1 D-12 invariant battery on the real `DEFAULTS` (Pitfall 4 — no finite-pie artifact).

Purpose: Sourcing becomes an enforced invariant, dragStrength's credibility rests on a reproducible procedure (not the number), and calibration provably introduces no zero-sum artifact.
Output: `sourcing.test.ts`, `calibration.test.ts`, extended `invariants.test.ts`, finalized `dragStrength.value` in `defaults.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-empirical-data-parameter-calibration/02-CONTEXT.md
@.planning/phases/02-empirical-data-parameter-calibration/02-RESEARCH.md
@.planning/phases/02-empirical-data-parameter-calibration/02-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
Engine: `projectionEngine(inputs: Inputs, params: Params): ProjectionResult` from `src/core/engine.js`. `ProjectionResult.series: YearSnapshot[]`; `YearSnapshot.assetInflation` is the per-year asset-price-inflation haircut; `anchorWealth.{median,top10,top1,top01}` and `userWealth` are real terms.
Test helpers: `relErr`, `DIST_TOL` (1e-6), `makeSyntheticParams`, `syntheticInputs` from `src/core/__tests__/testUtils.js`. Golden tolerance scheme: loose `DIST_TOL` vs golden `<1e-9` — do NOT invent new epsilons.
`DEFAULTS` imported from `../../data/defaults.js` (tests/composition root inject data into core — core never imports data).
Enumeration analog: invariants.test.ts:54-79 (accumulate violations, assert .toHaveLength(0) with joined diagnostic). Negative-test analog: basis.test.ts:27-31. Invariant battery to re-run: invariants.test.ts:84-210 (it cases at ~90/126/165/189; `sumAnchors` 47-49).
The (now CR-01-hardened, Plan 01) `bisect` is reusable for the back-solve because asset-inflation-share is monotone increasing in dragStrength. If `bisect` is not exported, add a narrowly-scoped test-only export or a local monotone bisection in the test file.
Open question O-1 (RESEARCH:431): the exact "asset-inflation share of net-worth growth" numerator/denominator must be pinned in calibration.test.ts as a commented formula that provably maps to McKinsey's definition ("share of net-worth growth from asset-price inflation, ~1/5 from new saving/investment") — NOT the ~1.3x asset/GDP ratio (D-08 excludes it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-11 sourcing-completeness enforcement test + negative test</name>
  <files>src/core/__tests__/sourcing.test.ts</files>
  <read_first>
    - src/core/__tests__/invariants.test.ts:42-79 (enumeration-driven enforcement pattern: collect violations, assert empty with joined diagnostic)
    - src/core/__tests__/basis.test.ts:27-31 (negative "test-the-test" pattern)
    - .planning/phases/02-empirical-data-parameter-calibration/02-RESEARCH.md §"Sourcing-completeness test" (lines 369-392) and Pitfall 6 (empty/whitespace must fail)
    - src/data/defaults.ts (the DEFAULTS tree being enumerated — from Plan 03)
    - src/core/types.ts (SourceRecord required-field list — from Plan 02)
  </read_first>
  <behavior>
    - Every SourcedParam in DEFAULTS (anchors x4, returnByTier x4, dragStrength, savings) has all six required SourceRecord fields present and non-empty after trim; basis is 'real'|'nominal' -> test passes
    - A deliberately blanked field (e.g. figureUsed = '  ') on a copy of a DEFAULTS param -> assertion fails (negative test, mirrors basis-mismatch test-the-test)
    - An empty string '' on a required field fails (not just null/undefined) — Pitfall 6
    - Phase 1 synthetic fixtures are NOT enumerated (D-12: gate targets only the production frozen module)
  </behavior>
  <action>
    Create `src/core/__tests__/sourcing.test.ts`. Define `REQUIRED: (keyof SourceRecord)[] = ['sourceName','figureUsed','basis','definition','yearVintage','retrievedDate']`. Write an `enumerateSourcedParams(DEFAULTS)` walker yielding `[path, SourcedParam]` for every nested `SourcedParam` (anchors, returnByTier, dragStrength, savings — and any other; horizon is a plain number, skip it). Write `assertComplete(path, p)` that for each required field asserts a non-empty trimmed string, and that `basis` is in `['real','nominal']`; accumulate failures into a violations array and assert `.toHaveLength(0)` with a joined human-readable diagnostic (invariants.test.ts:54-79 shape). Add a NEGATIVE test: a shallow-cloned DEFAULTS param with one required field blanked to `'  '` must make `assertComplete` fail (`expect(() => assertComplete('bad', bad)).toThrow()` or an equivalent failing assertion). Target `DEFAULTS` ONLY (D-12). Add the decision-ID JSDoc header (DATA-04/D-11/D-12).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/core/__tests__/sourcing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/core/__tests__/sourcing.test.ts` exits 0 with the completeness test AND the negative test green
    - The test enumerates `DEFAULTS` (contains `DEFAULTS` and the six-field `REQUIRED` list)
    - The negative test proves an empty/whitespace field fails the gate (not only null)
    - Phase 1 synthetic fixtures are not enumerated (no reference to `makeSyntheticParams` in the enforcement assertion)
  </acceptance_criteria>
  <done>DATA-04 is an enforced invariant: a build with any incomplete shipped SourceRecord fails; the negative test proves the gate rejects empty/whitespace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dragStrength back-solve harness + freeze the back-solved value</name>
  <files>src/core/__tests__/calibration.test.ts, src/data/defaults.ts</files>
  <read_first>
    - src/core/__tests__/invariants.test.ts:84-210 (engine-output-assertion analog: run projectionEngine on a Params fixture, assert on result.series with relErr/tolerance + descriptive message)
    - src/core/__tests__/testUtils.ts:29,44-46 (DIST_TOL, relErr — do not invent new epsilons)
    - .planning/phases/02-empirical-data-parameter-calibration/02-RESEARCH.md §"dragStrength back-solve harness" (349-367), Open Questions (RESOLVED) O-1/O-2 (431-439)
    - src/core/types.ts YearSnapshot.assetInflation semantics (lines 188-194)
    - src/data/defaults.ts dragStrength placeholder note (from Plan 03)
  </read_first>
  <behavior>
    - assetInflationShare(dragStrength) over the 2000-2021-like baseline is monotone increasing in dragStrength (sanity check on two sample points)
    - Back-solving for assetInflationShare(d) - 0.80 = 0 via the hardened bisect yields a finite d in a documented bracket
    - The frozen DEFAULTS.dragStrength.value reproduces ~0.80 share to 2 decimals AND equals the back-solved d to ~4 decimals
  </behavior>
  <action>
    Create `src/core/__tests__/calibration.test.ts`. This task implements D-07 (dragStrength back-solved from a historical baseline run, not an asserted constant) and D-08 (back-solve target = McKinsey ~80% asset-inflation share, explicitly NOT the ~1.3x asset/GDP ratio). Pin O-1: define `assetInflationShare(dragStrength)` with an explicit commented formula = cumulative asset-inflation contribution over the window / total real net-worth growth over the same window, derived from `result.series` `assetInflation` vs total growth — and a comment stating this maps to McKinsey's "share of net-worth growth from asset-price inflation" (per D-08, NOT the ~1.3x asset/GDP ratio). Pin O-2: use the calibrated real `DEFAULTS` as the 2000-2021-like baseline (self-consistent; document this choice — D-07 historical-baseline run). Back-solve d with the CR-01-hardened `bisect` (asset-inflation share monotone in dragStrength); set `src/data/defaults.ts` `dragStrength.value` to the solved constant and update its `note` to record the procedure + the McKinsey ~80% target + that it is reproduced by this test. Assert `assetInflationShare(DEFAULTS.dragStrength.value)` is close to 0.80 (toBeCloseTo, 2 decimals) AND `DEFAULTS.dragStrength.value` ≈ solved (toBeCloseTo, ~4 decimals). Add a monotonicity sanity assertion (two sample points). Use `relErr`/`DIST_TOL` — no ad-hoc epsilons. Decision-ID JSDoc header (DATA-03/D-07/D-08). Leave the divergence sanity-check to Task 3 (do not assert it here).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/core/__tests__/calibration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/core/__tests__/calibration.test.ts` exits 0 with the back-solve + frozen-value reproduction tests green
    - `src/data/defaults.ts` `dragStrength.value` is a concrete back-solved number (no longer a placeholder) and its `note` records the back-solve procedure + McKinsey ~80% target (D-07/D-08)
    - calibration.test.ts contains a commented formula for "asset-inflation share" explicitly distinguished from the ~1.3x asset/GDP ratio (D-08)
    - The frozen-value reproduction assertion uses `toBeCloseTo(0.80, 2)` (or documented equivalent) and a back-solve-equality assertion
    - A monotonicity sanity assertion (two sample points) is present
    - No new ad-hoc epsilon constants introduced (uses `relErr`/`DIST_TOL`)
  </acceptance_criteria>
  <done>dragStrength is a frozen back-solved constant (D-07) whose back-solve target is the McKinsey ~80% share (D-08) and whose procedure is reproducible by this test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Divergence sanity-check on real DEFAULTS (success criterion 5, O-3)</name>
  <files>src/core/__tests__/calibration.test.ts</files>
  <read_first>
    - src/core/__tests__/invariants.test.ts:84-210 (engine-output-assertion analog: run projectionEngine, assert on result.series with descriptive message)
    - src/core/__tests__/testUtils.ts:29,44-46 (DIST_TOL, relErr — do not invent new epsilons)
    - .planning/phases/02-empirical-data-parameter-calibration/02-RESEARCH.md §"Divergence sanity-check" (394-406), Open Questions (RESOLVED) O-3 (441-443)
    - src/data/defaults.ts (the back-solved DEFAULTS after Task 2)
    - src/core/__tests__/calibration.test.ts (Task 2 file — extend it; reuse its `DEFAULTS` import and helpers)
  </read_first>
  <behavior>
    - On real DEFAULTS over the default horizon, every tier (anchors median/top10/top1/top01) is finite and positive
    - top01 anchor wealth is below a documented relative ceiling (a documented multiple of total distribution wealth); no tier negative
  </behavior>
  <action>
    Extend `src/core/__tests__/calibration.test.ts` with a divergence sanity-check (O-3, success criterion 5): run `projectionEngine` on `{ ...DEFAULTS }` over `DEFAULTS.horizon`; assert every tier is finite and positive and `top01` stays below a documented relative ceiling — `top01` ≤ a documented multiple of total distribution wealth — with the multiple's rationale stated in a one-line comment. Use `relErr`/`DIST_TOL` — no ad-hoc epsilons. Add the decision-ID JSDoc note (SC-5/O-3) to the new describe block. Keep Task 2's back-solve tests unmodified and green.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/core/__tests__/calibration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/core/__tests__/calibration.test.ts` exits 0 — Task 2 back-solve tests AND the divergence sanity-check green
    - The divergence sanity-check asserts all tiers finite/positive and top01 under a documented relative ceiling (multiple-of-total rationale stated in a one-line comment)
    - Task 2's back-solve/frozen-value assertions are unchanged and still green
    - No new ad-hoc epsilon constants introduced (uses `relErr`/`DIST_TOL`)
  </acceptance_criteria>
  <done>The divergence sanity-check (success criterion 5) passes on the back-solved real defaults within a documented relative ceiling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: D-09 re-run of the D-12 invariant battery on real DEFAULTS (Pitfall 4)</name>
  <files>src/core/__tests__/invariants.test.ts</files>
  <read_first>
    - src/core/__tests__/invariants.test.ts:84-210 (the MODEL-04 D-12 battery: drag=0 collapse ~90, non-conservation/no-transfer ~126, no negative real wealth ~165, monotone divergence ~189; sumAnchors 47-49)
    - .planning/research/PITFALLS.md Pitfall 4 (zero-sum/finite-pie — drag must stay non-conserved; no tier loss equals another's gain)
    - .planning/phases/02-empirical-data-parameter-calibration/02-CONTEXT.md D-09
    - src/data/defaults.ts (the back-solved DEFAULTS from Task 2)
  </read_first>
  <behavior>
    - With the back-solved DEFAULTS at dragStrength=0 the engine collapses to independent per-tier baseline
    - On real DEFAULTS: no tier's loss exactly equals another tier's gain (non-conservation; no transfer function)
    - On real DEFAULTS: aggregate real wealth can still grow; no tier forced to negative real wealth by drag alone
    - On real DEFAULTS: divergence remains monotone (consistent with the Phase 1 synthetic battery)
  </behavior>
  <action>
    Implements D-09 (calibration test re-asserts the Phase 1 D-12 non-conservation / infinite-growth invariants on the real back-solved defaults). Extend `src/core/__tests__/invariants.test.ts` in place: add a `describe('D-09: D-12 invariant battery re-run on real DEFAULTS (Pitfall 4)')` block (or parametrize the existing MODEL-04 block) that imports `DEFAULTS` from `../../data/defaults.js` and re-runs the four invariant cases (drag=0 collapse, non-conservation/no-transfer, no negative real wealth, monotone divergence) with `DEFAULTS` swapped in for `makeSyntheticParams()`. Reuse `sumAnchors` and the existing non-conservation reasoning unchanged — only the params source changes. Keep all existing synthetic cases green and unmodified. This proves the back-solved calibration introduced no finite-pie/zero-sum artifact (D-09).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/core/__tests__/invariants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/core/__tests__/invariants.test.ts` exits 0 — existing synthetic cases AND the new DEFAULTS-fed cases green
    - invariants.test.ts imports `DEFAULTS` and runs the four-case battery on it (D-09)
    - The non-conservation case asserts no tier's loss equals another's gain on the real defaults (Pitfall 4, D-09)
    - Existing Phase 1 synthetic invariant cases are unchanged (no assertion weakened)
    - `npx vitest run` (full suite) exits 0
  </acceptance_criteria>
  <done>The Phase 1 D-12 invariants hold on the back-solved real defaults (D-09) — calibration provably preserves non-conservation/infinite-growth (no finite-pie artifact).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data module -> enforcement tests | The completeness gate is the build-time integrity boundary for "is every shipped default cited?". No runtime/network/auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering / Repudiation (epistemic) | an uncited/empty-sourced default silently shipping (DATA-04 bypass) | mitigate | D-11 enumeration test fails the build on any missing/empty/whitespace required field; negative test proves the gate rejects `''`/whitespace (Pitfall 6). HIGH severity — blocks release. |
| T-02-09 | Tampering (integrity) | dragStrength tuned "until it looks right" | mitigate | dragStrength is back-solved against the McKinsey ~80% target via a reproducible bisect procedure; the test re-derives it and asserts the frozen value reproduces the target (Pattern 3 — credibility in the procedure, not the number). |
| T-02-10 | Tampering (integrity) | calibration silently introducing a finite-pie/zero-sum artifact | mitigate | D-09 re-runs the D-12 non-conservation/infinite-growth battery on the real back-solved defaults (Pitfall 4). HIGH severity — blocks if any tier's loss equals another's gain. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH §"Package Legitimacy Audit: Not applicable"). |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` (full suite) exits 0
- Sourcing-completeness gate + its negative test green (DATA-04 enforced)
- dragStrength back-solve reproduces McKinsey ~80% and equals the solved value
- D-12 invariant battery green on real DEFAULTS (no finite-pie artifact)
- Divergence sanity-check green; `grep -RnE "\b18\b" src/core src/data` returns nothing
</verification>

<success_criteria>
Sourcing is a build-enforced invariant, dragStrength is a reproducible back-solve against McKinsey ~80%, the real defaults preserve the Phase 1 non-conservation/infinite-growth invariants (no finite-pie artifact), and divergence stays within documented bounds — all five Phase 2 success criteria are satisfied on the real calibrated defaults.
</success_criteria>

<output>
Create `.planning/phases/02-empirical-data-parameter-calibration/02-04-SUMMARY.md` when done
</output>
</output>
