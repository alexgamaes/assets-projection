---
phase: 02-empirical-data-parameter-calibration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/core/types.ts
  - src/core/__tests__/testUtils.ts
autonomous: true
requirements: [DATA-01]
must_haves:
  truths:
    - "A structured SourceRecord type exists with six required fields plus optional note/url"
    - "SourcedParam.source is the structured SourceRecord (no longer string | null)"
    - "The entire Phase 1 test suite still compiles and stays green after the breaking type change"
  artifacts:
    - path: "src/core/types.ts"
      provides: "SourceRecord interface + evolved SourcedParam"
      contains: "interface SourceRecord"
    - path: "src/core/__tests__/testUtils.ts"
      provides: "synParam migrated to the new SourceRecord shape (synthetic, D-12 exempt)"
      contains: "SourceRecord"
  key_links:
    - from: "src/core/types.ts SourcedParam"
      to: "SourceRecord"
      via: "source: SourceRecord field"
      pattern: "source: SourceRecord"
---

<objective>
Evolve `SourcedParam.source: string | null` into a structured `SourceRecord` (D-10) with six required fields (`sourceName`, `figureUsed`, `basis`, `definition`, `yearVintage`, `retrievedDate`) plus optional `note`/`url`, and migrate every Phase 1 fixture that constructs a `SourcedParam` so the existing suite still compiles and stays green.

Purpose: `SourceRecord` is the typed contract Wave C/D and Phase 3 VIZ-06 depend on. The type change is breaking — without the fixture migration the Phase 1 suite goes red (the one real cross-cutting effect identified in RESEARCH §"Runtime State Inventory").
Output: `SourceRecord` in `types.ts`, evolved `SourcedParam`, and a migrated `synParam`/`makeSyntheticParams` in `testUtils.ts` (synthetic citations remain allowed per D-12 — the gate targets only the production module, added in Wave C).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-empirical-data-parameter-calibration/02-CONTEXT.md
@.planning/phases/02-empirical-data-parameter-calibration/02-PATTERNS.md

<interfaces>
Current SourcedParam (src/core/types.ts:52-61) — `source: string | null` to replace; keep `value`, `basis`, optional `note`. JSDoc-per-field convention from `Params` (97-133) and the file header (1-10) must be followed; reuse the existing `'real' | 'nominal'` union for `SourceRecord.basis` (machine-checkable, consistent with `assertReal`).

Target SourceRecord shape (RESEARCH 02-RESEARCH.md:297-315):
```typescript
export interface SourceRecord {
  sourceName: string;
  figureUsed: string;
  basis: 'real' | 'nominal';
  definition: string;
  yearVintage: string;
  retrievedDate: string;
  note?: string;   // optional: correction trail / survivorship / geo-vs-arith caveat
  url?: string;    // optional: primary-source link Phase 3 VIZ-06 footer renders
}
```
Current synParam helper (src/core/__tests__/testUtils.ts:84-86) builds `{ value, basis, source: null, note }` and is the ONLY place full Params/SourcedParam trees are constructed for tests. `Anchors`/`ReturnByTier`/`Params` all nest `SourcedParam`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SourceRecord and evolve SourcedParam in types.ts</name>
  <files>src/core/types.ts</files>
  <read_first>
    - src/core/types.ts (the file being modified — SourcedParam 52-61, assertReal 36-40, Params JSDoc style 97-133, file header 1-10)
    - .planning/phases/02-empirical-data-parameter-calibration/02-RESEARCH.md §"SourceRecord shape (D-10)" (lines 293-316)
    - .planning/phases/02-empirical-data-parameter-calibration/02-PATTERNS.md §"src/core/types.ts" (in-place evolution, JSDoc-per-field convention, D-10 inline citation)
  </read_first>
  <action>
    Add an exported `interface SourceRecord` with exactly the six required fields `sourceName`, `figureUsed`, `basis: 'real' | 'nominal'`, `definition`, `yearVintage`, `retrievedDate` (all `string` except `basis`), plus optional `note?: string` and `url?: string`. Reuse the `'real' | 'nominal'` union for `basis` (consistent with `assertReal`; keeps the Wave D completeness gate machine-checkable). Change `SourcedParam.source` from `string | null` to `source: SourceRecord`. Follow the file's one-`/** ... */`-per-field JSDoc convention; cite D-10 inline; keep the authoritative-export-contract framing from the header (this contract is consumed by Phases 3/4). Add a JSDoc line on `note`/`url` noting they carry the discarded-18pp correction trail (D-06) and the Phase 3 VIZ-06 footer link respectively. Do NOT introduce Zod or any runtime validation here (D-11 mandates a Vitest test, not a schema validator — added in Wave D).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/types.ts` contains `export interface SourceRecord` with the six required fields and optional `note`/`url`
    - `src/core/types.ts` contains `source: SourceRecord` in `SourcedParam` (no remaining `string | null` for `source`)
    - `grep -n "string | null" src/core/types.ts` shows no occurrence on the `source` field
    - `npx tsc --noEmit` will fail ONLY at the unmigrated fixture call sites (expected — fixed in Task 2); the types.ts file itself has no type errors
  </acceptance_criteria>
  <done>SourceRecord exists with the locked six-field shape; SourcedParam.source is structured; type-level contract complete.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Phase 1 fixtures (synParam) to the SourceRecord shape</name>
  <files>src/core/__tests__/testUtils.ts</files>
  <read_first>
    - src/core/__tests__/testUtils.ts (the file being modified — synParam 84-86, makeSyntheticParams 104-125, syntheticInputs, the "no value 18 anywhere" rule at 11-13/92-93)
    - .planning/phases/02-empirical-data-parameter-calibration/02-RESEARCH.md §"Runtime State Inventory" (the cross-cutting fixture-migration requirement, lines 244-256)
    - .planning/phases/02-empirical-data-parameter-calibration/02-CONTEXT.md D-12 (fixtures may keep synthetic source-less params — the gate targets only the production module; fixtures must still COMPILE under the new type)
  </read_first>
  <action>
    Update `synParam` so it returns a `SourcedParam` whose `source` is a synthetic-but-type-valid `SourceRecord` (D-12 permits synthetic citations in fixtures; the type no longer allows `null`). Use an obviously-synthetic constant SourceRecord — e.g. `sourceName: 'synthetic'`, `figureUsed: 'synthetic fixture value'`, `basis` mirroring the param basis, `definition: 'synthetic test fixture — not a real citation'`, `yearVintage: 'n/a (synthetic)'`, `retrievedDate: 'n/a (synthetic)'`. Keep all existing synthetic numeric values unchanged. Preserve the "no literal 18 anywhere" rule (testUtils.ts:11-13/92-93) — do not introduce the numeral 18 in any string. Then run the full suite and fix any OTHER Phase 1 test files that directly construct a `SourcedParam` with `source: string | null` so they compile under the new shape (search the test tree; route them through `synParam` where possible). Do NOT weaken any Phase 1 assertion — only adapt construction shape.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (no remaining `source: string | null` construction anywhere)
    - `npx vitest run` (full suite) exits 0 — every Phase 1 test green, no assertions weakened
    - `grep -RnE "\b18\b" src/core/__tests__/testUtils.ts` returns no literal 18
    - `grep -RnE "source:\s*null" src/core src/data 2>/dev/null` returns nothing
  </acceptance_criteria>
  <done>All fixtures compile and pass under the SourceRecord shape; full suite green; breaking type change absorbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — runtime) | No network/auth/user-input/I/O surface. The `SourceRecord` type is the integrity contract; risk class is epistemic, not AppSec. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering (integrity) | `SourcedParam.source` contract | mitigate | Structured `SourceRecord` replaces a free `string \| null` — an uncited default can no longer be expressed as `null`; every shipped value must populate six fields (enforced by the Wave D test). |
| T-02-04 | Repudiation (epistemic) | breaking type change to a Phase 1-consumed contract | mitigate | Fixture-migration task makes the breaking change explicit and keeps the full suite green, preventing a silent red/contract drift. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH §"Package Legitimacy Audit: Not applicable"). No Zod added (A1). |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` (full suite) exits 0
- `SourceRecord` exported with six required + two optional fields
- No `source: null` or `source: string | null` remaining anywhere in `src/`
</verification>

<success_criteria>
The `SourceRecord` typed contract exists in its locked six-field shape, `SourcedParam.source` is structured, and the entire Phase 1 suite compiles and passes under the breaking change — the type foundation Wave C/D and Phase 3 VIZ-06 build on.
</success_criteria>

<output>
Create `.planning/phases/02-empirical-data-parameter-calibration/02-02-SUMMARY.md` when done
</output>
