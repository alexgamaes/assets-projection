---
phase: 02-empirical-data-parameter-calibration
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/data/sources.ts
  - src/data/defaults.ts
  - .planning/PROJECT.md
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "A frozen DEFAULTS Params object exists where every shipped value carries a complete SourceRecord"
    - "The four return-by-tier anchors are triangulated point estimates from corrected primary literature with no literal 18 anywhere in the engine or data"
    - "The chosen wealth-level anchors satisfy top01/top1 < 10 so calibrateCurve does not throw (alpha > 1, D-03)"
    - "PROJECT.md no longer states the '~18pp gap' shorthand — it is corrected to the Fagereng raw ~500bp / ~10pp net-of-tax framing"
  artifacts:
    - path: "src/data/sources.ts"
      provides: "Citation registry: one SourceRecord-bearing entry per study"
      contains: "fagereng"
    - path: "src/data/defaults.ts"
      provides: "Object.freeze'd DEFAULTS Params with full SourceRecord per value + back-solve placeholder note for dragStrength"
      contains: "Object.freeze"
  key_links:
    - from: "src/data/defaults.ts"
      to: "src/data/sources.ts"
      via: "registry reference per value"
      pattern: "SOURCES"
    - from: ".planning/PROJECT.md"
      to: "corrected Fagereng framing"
      via: "rewritten Context bullet"
      pattern: "500bp"
---

<objective>
Produce the frozen, citation-annotated default parameter set: a `src/data/sources.ts` citation registry and a `src/data/defaults.ts` `Object.freeze`d `DEFAULTS` Params where every shipped value carries a complete `SourceRecord`. Calibrate the four return-by-tier anchors as triangulated central estimates from the CORRECTED primary literature (Fagereng raw ~500bp / ~10pp net-of-tax association — NOT a flat 18pp per-tier gap; Bach 2020, Saez-Zucman, JST as level anchors), with NO literal "18" anywhere. Choose wealth-level anchors satisfying `top01/top1 < 10` (D-03). Record the discarded-18pp misreading + why in each anchor's `note`. Correct the PROJECT.md "~18pp gap" shorthand (D-06 — closes the STATE.md cross-cutting blocker).

dragStrength carries a complete SourceRecord but its `value` is a placeholder this plan marks for back-solve; the actual number is derived and frozen in Plan 04 (Pattern 3: calibration-as-procedure).

Purpose: This is the empirical spine of the phase. Credibility rests on triangulated, table-level-defined figures and a visible correction trail — not on convenient numbers.
Output: `src/data/sources.ts`, `src/data/defaults.ts`, corrected `.planning/PROJECT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-empirical-data-parameter-calibration/02-CONTEXT.md
@.planning/phases/02-empirical-data-parameter-calibration/02-RESEARCH.md
@.planning/phases/02-empirical-data-parameter-calibration/02-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md

<interfaces>
`SourceRecord` and evolved `SourcedParam` are exported from `src/core/types.ts` (Plan 02). `Params` shape (src/core/types.ts:106-133): `anchors` (median/top10/top1/top01 SourcedParam), `returnByTier` (same 4), `dragStrength` (SourcedParam), `horizon: number`, `distributionEvolution: 'endogenous' | 'fixed-shape-scaled'`, `savings` (SourcedParam).
Synthetic-tree analog to mirror structurally (NOT values): src/core/__tests__/testUtils.ts:104-125 makeSyntheticParams.
Architecture boundary (HARD): `core/` must NEVER import `src/data/*` (enforced by invariants.test.ts:42-79 FORBIDDEN scan). `data/` is injected into the engine by tests/composition root only.
Constraint to verify before freezing: `alpha = Math.log(10) / Math.log(top01.value / top1.value)` MUST be > 1, i.e. top01/top1 < 10 (D-03 / Pitfall 3).
PROJECT.md target: line 63 currently `Fagereng ... ~18pp gap between 10th and 90th percentile returns.` — this is the STATE.md cross-cutting blocker.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/data/sources.ts citation registry</name>
  <files>src/data/sources.ts</files>
  <read_first>
    - .planning/research/PITFALLS.md Pitfall 1 (corrected Fagereng framing), Pitfall 4 (geo vs arithmetic), Pitfall 7 (survivorship)
    - .planning/phases/02-empirical-data-parameter-calibration/02-RESEARCH.md §"defaults.ts shape" (registry-per-study pattern, lines 318-347) and §"Pitfall 1/4/5" (the corrected figures + caveats to record)
    - .planning/PROJECT.md lines 60-67 (the five named sources: Fagereng/Bach/Saez-Zucman/JST/Piketty + McKinsey)
    - src/core/types.ts (SourceRecord shape from Plan 02)
  </read_first>
  <action>
    Create `src/data/sources.ts` exporting a `SOURCES` registry constant (`as const`) with one entry per study: `fagereng2020`, `bach2020`, `saezZucman`, `jst2019`, `mckinsey2023`. Each entry is a complete `SourceRecord` (six required fields populated from primary-source table-level definitions — population, percentile type, gross/net, real/nominal, period; not a secondary summary). The Fagereng entry's `figureUsed` MUST state the corrected framing (raw ~500bp / ~10pp net-of-tax 10th->90th net-worth association as gradient justification) and MUST NOT contain the numeral 18 as a code literal; if the discarded misreading is referenced, phrase it without the digits "18" so the scoped grep gate passes (e.g. "the gross-of-tax association figure"). Add `url` where a primary-source link exists (Phase 3 VIZ-06 footer). Record survivorship caveat (JST) and geometric/arithmetic basis (JST/returns figures) in `note`/`definition` per Pitfalls 4/7. Add the decision-ID JSDoc header (DATA-01/D-10/D-04/D-05). This module is data — it must NOT import from `core/`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && ! grep -RnE "\b18\b" src/data/sources.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/sources.ts` exists and `npx tsc --noEmit` exits 0
    - `SOURCES` has entries `fagereng2020`, `bach2020`, `saezZucman`, `jst2019`, `mckinsey2023`, each typed as `SourceRecord`
    - `grep -RnE "\b18\b" src/data/sources.ts` returns nothing (no literal 18)
    - Fagereng entry `figureUsed` contains the substring `500bp` or `net-of-tax` (corrected framing, not the flat-gap misreading)
    - `grep -n "from ['\"].*core" src/data/sources.ts` shows no import OF data BY core and no core->data violation introduced
  </acceptance_criteria>
  <done>Citation registry exists with five complete SourceRecord entries reflecting the corrected Fagereng framing and required caveats; tsc clean; no literal 18.</done>
</task>

<task type="auto">
  <name>Task 2: Create frozen src/data/defaults.ts with triangulated anchors</name>
  <files>src/data/defaults.ts</files>
  <read_first>
    - src/core/__tests__/testUtils.ts:104-125 (Params tree structure to mirror — keys only, NOT synthetic values)
    - .planning/phases/02-empirical-data-parameter-calibration/02-RESEARCH.md §"defaults.ts shape" + §"Pitfall 1/3/4/5" (triangulation rule D-04/D-05, alpha>1 precondition D-03, geo-vs-arith)
    - .planning/PROJECT.md line 67 indicative bands (median ~2-3%, top10 ~4-5%, top1 ~6-9%, top01 ~10-15%+) — starting bands to triangulate within, NOT to copy blindly
    - .planning/phases/02-empirical-data-parameter-calibration/02-CONTEXT.md D-04, D-05, D-06, D-12 (central/midpoint triangulated estimate; per-anchor discarded-18pp note; horizon is a UX default not DATA-04-scoped)
  </read_first>
  <action>
    Create `src/data/defaults.ts` exporting `DEFAULTS` wrapped in `Object.freeze(...)`, structurally matching `Params`. For `returnByTier` median/top10/top1/top01 and `anchors` median/top10/top1/top01: choose triangulated CENTRAL estimates from the corrected literature (Fagereng correction sizes the median->top GRADIENT only; Bach leverage/systematic-risk, Saez-Zucman top-tail, JST asset base place the LEVELS — D-05), each with a complete `SourceRecord` referencing `SOURCES` and a `note` recording (a) the range it was drawn from (D-04), (b) the discarded "~18pp flat per-tier gap" misreading and WHY it is wrong — phrased WITHOUT the numeral 18 as a literal (D-06), (c) survivorship/geo-vs-arith caveat where applicable. Set `dragStrength` with a complete SourceRecord (mckinsey2023, `figureUsed` = the ~80% asset-inflation-share figure) but mark `value` explicitly as a back-solve placeholder in its `note` ("Back-solved in calibration.test.ts — not asserted; see Plan 04"); use a clearly-provisional value. Set `savings`, `distributionEvolution: 'endogenous'`, and `horizon` to a planner-chosen value within ENTRY-03's ~30-40y band (UX default, NOT DATA-04-scoped — its SourceRecord may state it is a non-sourced UX default). BEFORE finishing, compute `alpha = Math.log(10)/Math.log(top01.value/top1.value)` and confirm `alpha > 1` (top01/top1 < 10, D-03) — adjust the level anchors if not. No literal 18 anywhere. This module is data — NEVER imported by `core/`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && ! grep -RnE "\b18\b" src/data/defaults.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/defaults.ts` exists, `npx tsc --noEmit` exits 0
    - `DEFAULTS` is wrapped in `Object.freeze(`
    - `grep -RnE "\b18\b" src/data src/core` returns nothing (no literal 18 in engine or data)
    - Each `returnByTier`/`anchors` entry has a non-empty `source` of every required field and a `note` referencing the discarded misreading (without the digits 18)
    - The chosen anchors satisfy top01/top1 < 10 (documented in a comment with the computed alpha > 1)
    - `dragStrength.note` explicitly states the value is a Plan-04 back-solve placeholder
  </acceptance_criteria>
  <done>Frozen DEFAULTS exists with triangulated, fully-sourced anchors satisfying alpha>1, no literal 18, dragStrength marked for back-solve.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the PROJECT.md ~18pp shorthand (D-06, close STATE.md blocker)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md lines 60-67 (the Fagereng Context bullet at line 63 — the STATE.md cross-cutting blocker)
    - .planning/STATE.md §"Blockers/Concerns" ("PROJECT.md still contains the imprecise ~18pp gap shorthand — must be corrected before Phase 2 closes")
    - .planning/phases/02-empirical-data-parameter-calibration/02-RESEARCH.md §"State of the Art" (the exact corrected framing wording)
  </read_first>
  <action>
    Rewrite the Fagereng Context bullet in `.planning/PROJECT.md` (currently line ~63: "...~18pp gap between 10th and 90th percentile returns.") to the corrected framing: the raw cross-sectional 10th-90th return spread is ~500bp; the larger figure is a gross-of-tax / ~10pp net-of-tax ASSOCIATION across net-worth percentiles, NOT a per-tier real-return gap — and is used only as qualitative gradient justification, with per-tier levels triangulated from Bach/Saez-Zucman/JST. Do NOT use the numeral 18 as a literal in the rewritten text (describe it as the discarded gross-of-tax association without the digits). This is documentation prose (not engine code) so phrasing latitude is fine, but the "18" digit must not remain. Do not alter unrelated PROJECT.md content.
  </action>
  <verify>
    <automated>! grep -nE "18pp|\b18\b" .planning/PROJECT.md | grep -i fagereng</automated>
  </verify>
  <acceptance_criteria>
    - The Fagereng bullet in `.planning/PROJECT.md` no longer contains `18pp` or the digits `18`
    - The bullet contains the corrected framing (mentions ~500bp raw spread and net-of-tax association as gradient justification, not a per-tier gap)
    - `grep -n "18" .planning/PROJECT.md` shows the Fagereng line is no longer among the matches (other unrelated lines, e.g. JST date ranges, may remain)
    - No unrelated PROJECT.md sections changed (diff scoped to the Fagereng bullet)
  </acceptance_criteria>
  <done>The STATE.md cross-cutting ~18pp blocker is closed: PROJECT.md states the corrected Fagereng framing with no literal 18 in the Fagereng bullet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| literature -> data module | Untrusted (epistemic) crossing: a primary figure can be misread or mis-defined. The corrected-Fagereng correction trail + table-level definitions are the integrity control. No runtime/network/auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering (epistemic integrity) | Fagereng anchor calibration | mitigate | Corrected ~500bp/~10pp framing enforced; NO literal 18 anywhere (scoped grep gate); per-anchor `note` records the discarded misreading + why (visible correction trail). HIGH severity — blocks if a literal 18 reaches engine/data. |
| T-02-06 | Tampering (integrity) | extreme top-heavy anchors re-tripping CR-02 | mitigate | top01/top1 < 10 verified before freezing (alpha > 1, D-03); the Plan 01 guard fails loud if a later edit violates it. |
| T-02-07 | Repudiation (epistemic) | uncited / mis-defined default silently shipped | mitigate | Every value carries a complete SourceRecord traced to a primary-source table-level definition (population/percentile/gross-net/real-nominal); survivorship + geo-vs-arith caveats recorded (Pitfalls 4/7). Wave D test enforces completeness. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH §"Package Legitimacy Audit: Not applicable"). |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `grep -RnE "\b18\b" src/core src/data` returns nothing (success criterion 3)
- `DEFAULTS` is `Object.freeze`d with a complete SourceRecord per shipped value
- top01/top1 < 10 (alpha > 1) for the chosen anchors
- PROJECT.md Fagereng bullet corrected, no literal 18 (STATE.md blocker closed)
</verification>

<success_criteria>
A frozen, fully-sourced default parameter set exists with triangulated anchors from the corrected literature, no literal 18 anywhere in engine or data, anchors satisfying alpha>1, and the PROJECT.md ~18pp cross-cutting blocker closed — the empirical foundation Plan 04 enforces and back-solves against.
</success_criteria>

<output>
Create `.planning/phases/02-empirical-data-parameter-calibration/02-03-SUMMARY.md` when done
</output>
