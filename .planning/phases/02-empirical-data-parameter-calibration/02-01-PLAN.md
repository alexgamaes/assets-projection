---
phase: 02-empirical-data-parameter-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/distribution.ts
  - src/core/__tests__/distribution.test.ts
autonomous: true
requirements: [DATA-02, DATA-03]
must_haves:
  truths:
    - "D-01: Both carried-forward latent bugs (CR-01 bisect, CR-02 alpha) are guarded as the FIRST work of the phase, before any real empirical data is loaded"
    - "D-02: bisect() throws a diagnostic Error (no silent clamp) when the root is not bracketed or an endpoint is non-finite, instead of returning a wrong root"
    - "D-02: calibrateCurve() throws a diagnostic Error naming the offending alpha/ratio (no silent clamp) when the Pareto tail index alpha <= 1 (top01/top1 >= 10), instead of producing negative/garbage total wealth"
    - "The full Phase 1 test suite remains green after the guards are added"
  artifacts:
    - path: "src/core/distribution.ts"
      provides: "CR-01 bisect bracket/finite guard + CR-02 alpha>1 domain guard"
      contains: "root not bracketed"
    - path: "src/core/__tests__/distribution.test.ts"
      provides: "Negative tests asserting both guards throw"
      contains: "toThrow"
  key_links:
    - from: "src/core/distribution.ts calibrateCurve"
      to: "alpha derivation site (after Math.log(10)/Math.log(top01/top1))"
      via: "if (!(alpha > 1)) throw"
      pattern: "alpha > 1"
---

<objective>
Harden the two carried-forward latent silent-failure bugs (CR-01 bisect non-bracketed root, CR-02 Pareto alpha<=1) in `src/core/distribution.ts` with fail-loud guards, plus their negative tests. This is the mandatory FIRST work of the phase (D-01: fix BOTH latent bugs before any real empirical data is loaded): the guards must merge green BEFORE any real empirical anchor/drag numbers are introduced, so a bad calibration fails loudly instead of silently poisoning the curve.

Purpose: Without these guards, real top-heavy anchors (exactly what this app models) silently produce a confidently-wrong projection. The guards are what make Wave C calibration safe.
Output: Two fail-loud guards (assertReal-shaped) in `distribution.ts` and paired negative tests in `distribution.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-empirical-data-parameter-calibration/02-CONTEXT.md
@.planning/phases/02-empirical-data-parameter-calibration/02-PATTERNS.md
@.planning/phases/01-model-foundation/01-REVIEW.md

<interfaces>
From src/core/types.ts (the fail-loud precedent the new guards must mirror exactly — descriptive Error naming value + violated bound + parameter, never clamp):
```typescript
export function assertReal(p: { basis: 'real' | 'nominal' }, ctx: string): void {
  if (p.basis !== 'real') {
    throw new Error(`Basis violation in ${ctx}: expected real, got ${p.basis}`);
  }
}
```
CR-01 site: src/core/distribution.ts bisect() at lines 584-609 (currently computes only `fa = f(a)`, no bracket/finite check).
CR-02 site: src/core/distribution.ts calibrateCurve() immediately after `const alpha = Math.log(10) / Math.log(top01Wealth / top1Wealth);` (~line 292). `paretoConditionalMean` (235-237) divides by `(alpha - 1)`.
distribution.test.ts already imports `calibrateCurve` and `Anchors`/`ReturnByTier` types from `../distribution.js`/`../types.js`; negative-test pattern analog is src/core/__tests__/basis.test.ts:27-31 (`.toThrow(/regex/)`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CR-01 bisect bracket/finite guard + CR-02 alpha>1 guard</name>
  <files>src/core/distribution.ts</files>
  <read_first>
    - src/core/distribution.ts (the file being modified — read bisect() ~584-609, calibrateCurve alpha derivation ~292, paretoConditionalMean ~235-237)
    - src/core/types.ts:36-40 (assertReal fail-loud shape to mirror — already in <interfaces>)
    - .planning/phases/01-model-foundation/01-REVIEW.md §CR-01 (lines 52-82) and §CR-02 (lines 84-106) — verbatim fix shapes and exact bug locations
    - .planning/phases/02-empirical-data-parameter-calibration/02-PATTERNS.md §"src/core/distribution.ts" (guard-site line numbers + error-message convention)
  </read_first>
  <behavior>
    - bisect with non-finite endpoint: f returns NaN/Infinity at lo or hi -> throws Error matching /bisect: non-finite endpoint/
    - bisect with unbracketed root: f(lo) and f(hi) share the same sign -> throws Error matching /bisect: root not bracketed in \[.*\]/ including the f(lo)/f(hi) values
    - bisect with exact zero at an endpoint: returns that endpoint (fa===0 -> return a; fb===0 -> return b)
    - bisect with a properly bracketed root: unchanged behavior, existing loop produces the same root as before
    - calibrateCurve with anchors giving top01/top1 >= 10 (alpha <= 1): throws Error matching /alpha=.*≤ 1/ naming the offending alpha, the top01/top1 ratio, and the "Constrain anchors so top01/top1 < 10" remediation
    - calibrateCurve with valid anchors (alpha > 1): unchanged behavior
  </behavior>
  <action>
    This task implements D-01 (both guards added as the first work of the phase, before any real data) and D-02 (fail-loud throw with diagnostic message, no silent clamp). In `bisect()` (~line 584): after `const fa = f(a);` add `const fb = f(b);`; then, BEFORE the existing loop, in this order: (1) `if (!Number.isFinite(fa) || !Number.isFinite(fb)) throw` with message `bisect: non-finite endpoint f(${lo})=${fa}, f(${hi})=${fb}`; (2) `if (fa === 0) return a;`; (3) `if (fb === 0) return b;`; (4) `if (fa * fb > 0) throw` with message `bisect: root not bracketed in [${lo}, ${hi}] (f(lo)=${fa}, f(hi)=${fb})`. Leave the existing loop body unchanged. In `calibrateCurve()`, immediately AFTER the line `const alpha = Math.log(10) / Math.log(top01Wealth / top1Wealth);` (~line 292) add `if (!(alpha > 1)) throw new Error(...)` with message `calibrateCurve: Pareto tail index alpha=${alpha} ≤ 1 (top01/top1=${top01Wealth / top1Wealth}); mean wealth is undefined for alpha ≤ 1. Constrain anchors so top01/top1 < 10.`. Use the literal `≤` character to match 01-REVIEW.md and the planned test regex. Per D-02 both guards must throw a descriptive Error (assertReal shape) — NO silent clamp, NO fallback return. Add/extend the JSDoc header noting CR-01/CR-02/D-01/D-02 coverage.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/core/__tests__/distribution.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `src/core/distribution.ts` contains the substring `root not bracketed` and the substring `non-finite endpoint`
    - `src/core/distribution.ts` contains a guard `if (!(alpha > 1))` immediately after the alpha derivation line
    - No silent clamp introduced (D-02): `grep -nE "Math.min|Math.max|clamp" src/core/distribution.ts` shows no new clamp on alpha or bisect endpoints (existing unrelated clamps unchanged)
    - `npx vitest run` (full suite) exits 0 — existing Phase 1 tests still green
  </acceptance_criteria>
  <done>Both guards present at the specified sites with assertReal-shaped diagnostic messages (D-02); tsc clean; full Phase 1 suite still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CR-01/CR-02 negative tests to distribution.test.ts</name>
  <files>src/core/__tests__/distribution.test.ts</files>
  <read_first>
    - src/core/__tests__/distribution.test.ts (the file being modified — header lines 1-44 enumerate covered behaviors + hand-computed fixtures; imports lines 45-56)
    - src/core/__tests__/basis.test.ts:27-31 (canonical `.toThrow(/regex/)` negative-test analog)
    - .planning/phases/02-empirical-data-parameter-calibration/02-PATTERNS.md §"distribution.test.ts" (extend, do not create a new file)
  </read_first>
  <behavior>
    - Test: `calibrateCurve` with anchors where top01/top1 >= 10 throws matching /alpha=.*≤ 1/
    - Test: a deliberately unbracketed bisect call throws matching /root not bracketed/ (exercised through a public path that invokes bisect with anchors/inputs forcing the stitch root or 50%-share percentile outside the hardcoded bracket; if bisect is not directly exported, drive it via calibrateCurve/dynamicTopSetPercentile with anchors documented to fall outside [0.9001,0.9999]/[0.0001,0.9999])
    - Test: valid anchors do NOT throw (regression guard so the negative tests prove the guard is conditional)
  </behavior>
  <action>
    Extend `src/core/__tests__/distribution.test.ts` in place (do NOT create a new file). Add a `describe('CR-01/CR-02 fail-loud guards (D-01/D-02)')` block (these negative tests prove the D-02 fail-loud behavior of the D-01 guards) with: (1) `expect(() => calibrateCurve(badAnchors)).toThrow(/alpha=.*≤ 1/)` using anchors with `top01/top1 >= 10` (e.g. top1 and top01 chosen so the ratio is >= 10 — document the chosen values and the computed alpha in a comment per the file's hand-computed-fixture convention); (2) an unbracketed-bisect throw assertion driven through the lowest-friction public entry that calls bisect with out-of-bracket data, asserting `.toThrow(/root not bracketed/)`; (3) a positive control asserting valid anchors do not throw. Extend the file's doc-comment header to list the new CR-01/CR-02 cases in the existing style. Use the literal `≤` in the regex to match the guard message.
  </action>
  <verify>
    <automated>npx vitest run src/core/__tests__/distribution.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/core/__tests__/distribution.test.ts` exits 0 with the new cases passing
    - The test file contains `.toThrow(/alpha=.*≤ 1/)` and `.toThrow(/root not bracketed/)` (or equivalent regexes on the exact guard messages)
    - The test file contains a positive-control assertion that valid anchors do NOT throw
    - `npx vitest run` (full suite) exits 0
  </acceptance_criteria>
  <done>Negative tests for both guards plus a positive control are green; full suite green; this plan can merge before any real data exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — runtime) | This phase has no network/auth/user-input/I/O surface. Data is build-time-frozen; core is pure. The real risk class is epistemic integrity, not classical AppSec. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (integrity) | `bisect()` / `calibrateCurve()` numeric core | mitigate | CR-01/CR-02 fail-loud guards: a bad/extreme anchor set throws a diagnostic Error instead of silently producing a confidently-wrong curve. Negative tests prove the guards fire. |
| T-02-02 | Repudiation (epistemic) | silent wrong-root / negative-mean output | mitigate | No silent clamp (D-02) — every domain violation is loud and named, leaving an unambiguous failure signal rather than plausible junk. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO new packages (RESEARCH §"Package Legitimacy Audit: Not applicable"). No supply-chain surface introduced. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` (full suite) exits 0 — Phase 1 suite must remain green with the new guards
- Both guard messages literally present in `src/core/distribution.ts`
- No new silent clamp/fallback on the guarded values
</verification>

<success_criteria>
CR-01 and CR-02 guards are implemented fail-loud (assertReal shape, no clamp) at the exact 01-REVIEW.md sites, paired with green negative tests, and the entire Phase 1 suite stays green — establishing the safety net required before any Wave C empirical numbers are written.
</success_criteria>

<output>
Create `.planning/phases/02-empirical-data-parameter-calibration/02-01-SUMMARY.md` when done
</output>
</output>
