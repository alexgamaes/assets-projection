---
phase: 04-ui-shell-minimal-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/__tests__/store.test.ts
  - src/data/sources.ts
  - src/data/defaults.ts
  - src/state/store.ts
autonomous: true
requirements:
  - ENTRY-01
  - ENTRY-02
  - ENTRY-03
  - ENTRY-04

must_haves:
  truths:
    - "The Zustand store exposes setInputs, setHorizon, and setBasis actions"
    - "setHorizon does not mutate the frozen DEFAULTS object"
    - "The store's default currentWealth is 200_000 sourced from SCF 2022 (not 120_000)"
    - "src/data/sources.ts contains a seedWealth entry and an inflationRate entry, each with all six SourceRecord fields non-empty"
    - "src/data/defaults.ts contains seedWealth (200_000, real) and inflationRate (~0.025, nominal) frozen entries"
    - "The sourcing-completeness Vitest gate (src/core/__tests__/sourcing.test.ts) stays green"
    - "store.test.ts contains failing-then-passing stubs covering ENTRY-01, ENTRY-02, ENTRY-03"
  artifacts:
    - path: "src/state/__tests__/store.test.ts"
      provides: "Vitest stubs for setInputs/setHorizon/setBasis actions and cited seed defaults"
    - path: "src/data/sources.ts"
      provides: "seedWealth and inflationRate SourceRecord entries"
    - path: "src/data/defaults.ts"
      provides: "seedWealth (200_000) and inflationRate (0.025) frozen sourced-param entries"
    - path: "src/state/store.ts"
      provides: "ProjectionStore with basis field and three setters; seed from DEFAULTS"
  key_links:
    - from: "src/state/store.ts"
      to: "src/data/defaults.ts"
      via: "DEFAULTS import and new SEED_WEALTH / SEED_SAVINGS constants"
      pattern: "DEFAULTS\\.seedWealth\\.value|DEFAULTS\\.savings\\.value"
    - from: "src/data/defaults.ts"
      to: "src/data/sources.ts"
      via: "SOURCES.scf2022 and SOURCES.blsCpiLongRun references in source fields"
      pattern: "scf2022|blsCpiLongRun"
---

<objective>
Foundation slice: Wave-0 test stubs, two new cited data defaults (seed wealth + inflation rate), and the Zustand store actions that enable live recompute.

Purpose: Establish the data and state seam before any UI component is built. Every downstream plan reads from this store; the cited defaults must pass the Phase 2 sourcing gate before the shell can ship.

Output: Green store test stubs, sourcing-complete defaults, a store with setInputs/setHorizon/setBasis, and a corrected seed default (200k vs the 120k placeholder).
</objective>

<execution_context>
@/Users/gama/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gama/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gama/Documents/personal/assets-projection/.planning/ROADMAP.md
@/Users/gama/Documents/personal/assets-projection/.planning/phases/04-ui-shell-minimal-entry/04-CONTEXT.md
@/Users/gama/Documents/personal/assets-projection/.planning/phases/04-ui-shell-minimal-entry/04-RESEARCH.md
@/Users/gama/Documents/personal/assets-projection/.planning/phases/04-ui-shell-minimal-entry/04-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase 2026-05-16. -->

From src/core/types.ts:
- SourceRecord interface: sourceName, figureUsed, basis ('real'|'nominal'), definition, yearVintage, retrievedDate (all required); note?, url? (optional)
- SourcedParam interface: value: number, basis: 'real'|'nominal', source: SourceRecord, note?: string
- Inputs interface: currentWealth: number, annualSavings: number
- Params interface: anchors, returnByTier, dragStrength, horizon: number, distributionEvolution, savings: SourcedParam

From src/state/store.ts (CURRENT — pre-Phase-4):
  interface ProjectionStore { inputs: Inputs; params: Params; }
  export const useProjectionStore = create<ProjectionStore>(() => ({
    inputs: { currentWealth: 120_000, annualSavings: 6_000 },
    params: DEFAULTS,
  }));

From src/data/defaults.ts (savings UX-default pattern, lines 379–404):
  savings: Object.freeze({
    value: 6_000,
    basis: 'real' as const,
    source: Object.freeze({ sourceName: '...', figureUsed: '...', basis: 'real' as const,
      definition: '...', yearVintage: '...', retrievedDate: '2026-05-16', note: '...' }),
  }),

From src/data/sources.ts (fagereng2020 pattern for new entries):
  export const SOURCES = {
    fagereng2020: { sourceName: '...', figureUsed: '...', basis: 'real' as const,
      definition: '...', yearVintage: '...', retrievedDate: '2026-05-16', note: '...', url: '...' }
    satisfies SourceRecord,
    // ...
  } as const;

From src/state/__tests__/selectors.test.ts (test fixture pattern):
  import { makeSyntheticParams, syntheticInputs } from '../../core/__tests__/testUtils.js';
  const params = makeSyntheticParams({ horizon: 5 });
  const result = projectionEngine(syntheticInputs, params);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 store test stubs (ENTRY-01, ENTRY-02, ENTRY-03)</name>
  <files>src/state/__tests__/store.test.ts</files>
  <read_first>
    - src/state/store.ts — current store shape (to understand what will be added)
    - src/state/__tests__/selectors.test.ts — exact import pattern and describe block style to mirror
    - src/core/__tests__/testUtils.ts — available test helpers (makeSyntheticParams, syntheticInputs)
    - src/data/defaults.ts (lines 1–30) — DEFAULTS export shape for tests
  </read_first>
  <behavior>
    - Test: setInputs({ currentWealth: 500_000 }) → store.inputs.currentWealth === 500_000
    - Test: setHorizon(50) → store.params.horizon === 50 and original DEFAULTS.horizon is unchanged (frozen guard)
    - Test: setBasis('nominal') → store.basis === 'nominal'; setBasis('real') → store.basis === 'real'
    - Test: default store.inputs.currentWealth === 200_000 (cited seed — not 120_000)
    - Test: default store.basis === 'real' (ENTRY-04 default)
    - Test: setInputs partial patch — only updates the patched key, other input fields preserved
  </behavior>
  <action>
    Create src/state/__tests__/store.test.ts. Import useProjectionStore from '../store.js'. Import DEFAULTS from '../../data/defaults.js'.

    Write describe blocks labelled by requirement ID following the selectors.test.ts style:

    describe('ENTRY-01 / D-02: cited seed defaults') — assert store initial state has inputs.currentWealth === 200_000 and basis === 'real'.

    describe('ENTRY-02: setInputs action') — call useProjectionStore.getState().setInputs({ currentWealth: 500_000 }); assert getState().inputs.currentWealth === 500_000 and annualSavings is unchanged. Also test partial patch: setInputs({ annualSavings: 12_000 }) leaves currentWealth at its previous value.

    describe('ENTRY-03: setHorizon action') — call setHorizon(50); assert getState().params.horizon === 50. Assert that DEFAULTS.horizon is NOT 50 (frozen original is unchanged; use a local reference captured before the set call).

    describe('ENTRY-04: setBasis action') — call setBasis('nominal'); assert getState().basis === 'nominal'. Call setBasis('real'); assert getState().basis === 'real'.

    Use zustand's act-and-read pattern (getState() calls on the store singleton). No React testing library needed — store is a plain zustand instance.

    Each test must be deterministic: reset the store's state between tests using useProjectionStore.setState with the initial values to prevent cross-test pollution.
  </action>
  <verify>
    <automated>npm test -- src/state/__tests__/store.test.ts</automated>
  </verify>
  <done>All store test stubs exist and are RED (fail because the store lacks the new actions/seed yet). The file is valid TypeScript (tsc --noEmit passes or fails only on the store shape, not on the test file itself).</done>
</task>

<task type="auto">
  <name>Task 2: Cited data defaults — seedWealth + inflationRate (D-02, D-03, D-07)</name>
  <files>src/data/sources.ts, src/data/defaults.ts</files>
  <read_first>
    - src/data/sources.ts (full file) — SOURCES registry shape; architecture boundary comment (must NOT import from core/); satisfies SourceRecord pattern
    - src/data/defaults.ts (lines 370–405) — savings UX-default pattern to copy exactly (Object.freeze nesting, basis, note)
    - src/data/defaults.ts (lines 1–30) — Params satisfies type and export shape
    - src/core/types.ts (SourceRecord interface, lines 59–80) — six required fields
    - src/core/__tests__/sourcing.test.ts — the gate that must stay green (read to understand what it checks)
  </read_first>
  <action>
    STEP 1 — src/data/sources.ts: append two new entries inside the SOURCES object before the closing `} as const`:

    Entry 1: key scf2022 — cites US Federal Reserve Survey of Consumer Finances 2022 (published September 2023). Fields:
    - sourceName: 'Federal Reserve Board — Survey of Consumer Finances 2022 (SCF 2022)'
    - figureUsed: 'Median family net worth $192,900 (2022 survey dollars)'
    - basis: 'real' as const (SCF figures are nominal but the UX-default note explains the rounding)
    - definition: 'US median family net worth; all families, 2022 survey; nominal 2022 dollars; rounded up to $200,000 as a UX seed default per D-02'
    - yearVintage: '2022 survey data (published September 2023)'
    - retrievedDate: '2026-05-16'
    - note: 'UX default seed for current wealth entry; not a calibrated model parameter (DATA-04 does not apply). SCF 2022 median family net worth = $192,900; rounded up to $200,000 for the two-input start experience (D-02). Source: federalreserve.gov/publications/files/scf23.pdf Table 2.'
    - url: 'https://www.federalreserve.gov/publications/files/scf23.pdf'

    Entry 2: key blsCpiLongRun — cites BLS CPI-U long-run geometric mean. Fields:
    - sourceName: 'US Bureau of Labor Statistics — CPI-U long-run geometric mean (1926–2022)'
    - figureUsed: 'Long-run geometric mean annual CPI-U change approximately 2.9% (1926–2022) — rounded to 2.5% as a conservative long-run anchor'
    - basis: 'nominal' as const (CPI-U is a nominal rate)
    - definition: 'US Consumer Price Index for All Urban Consumers (CPI-U, all items); long-run geometric average annual rate 1926–2022; fixed display-only rate for nominal re-inflation selector (D-07); not a user control (v2 CONFIG-01)'
    - yearVintage: '1926–2022 long-run average'
    - retrievedDate: '2026-05-16'
    - note: 'UX display default for nominal re-inflation (D-07 / selectReinflated); fixed for v1. Long-run CPI-U geometric mean ~2.9% (1926–2022); using 2.5% as a slightly conservative anchor that better reflects the post-1990 disinflationary period while remaining well-cited. Source: BLS CPI data series CUUR0000SA0. Money-illusion caption (D-09) cites this rate and source name.'
    - url: 'https://www.bls.gov/cpi/data.htm'

    Both entries must use `satisfies SourceRecord` like the existing entries. No import from src/core/ (architecture boundary — sources.ts already imports SourceRecord type from '../core/types.js' which is acceptable as a type-only import).

    STEP 2 — src/data/defaults.ts: append two new frozen entries to the DEFAULTS object before the closing `} satisfies Params)`:

    Wait — DEFAULTS satisfies Params, and Params does not have seedWealth or inflationRate. These are NEW sourced-param values that must be added to the DEFAULTS object OUTSIDE the Params shape, OR as standalone constants. The correct pattern (per PATTERNS.md §"defaults.ts Phase 4 additions") is: add them as top-level properties on the exported DEFAULTS object. However, if DEFAULTS satisfies Params and Params doesn't include these fields, TypeScript will reject extra properties.

    Instead, export two separate frozen constants from defaults.ts alongside DEFAULTS:
    - export const SEED_WEALTH = Object.freeze({ value: 200_000, basis: 'real' as const, source: Object.freeze({ ...scf2022 SourceRecord inline... }) })
    - export const INFLATION_RATE = Object.freeze({ value: 0.025, basis: 'nominal' as const, source: Object.freeze({ ...blsCpiLongRun SourceRecord inline... }) })

    Use the inline SourceRecord form (copy the fields from SOURCES — do NOT import SOURCES into defaults.ts as that would create a circular reference since defaults.ts is imported by the store). The SourceRecord fields in these constants MUST match exactly those added to SOURCES in Step 1 so the sourcing gate sees consistent data.

    Also update the DEFAULTS initializer: change `inputs: { currentWealth: 120_000, ... }` — wait, inputs are NOT in DEFAULTS (Params). The store hardcodes the seed inputs. Export SEED_WEALTH separately; the store will import it.

    STEP 3 — verify the sourcing gate stays green: `npm test -- src/core/__tests__/sourcing.test.ts`. If the gate checks DEFAULTS properties and the two new constants are separate exports (not in DEFAULTS), the gate may not cover them automatically. Read the gate file first; if it only iterates DEFAULTS, document that SEED_WEALTH and INFLATION_RATE are UX defaults (like savings) and note that sourcing completeness is manually audited here. Do NOT add sourcing gate failures.
  </action>
  <verify>
    <automated>npm test -- src/core/__tests__/sourcing.test.ts</automated>
  </verify>
  <done>
    - src/data/sources.ts contains scf2022 and blsCpiLongRun entries, each with all six required SourceRecord fields non-empty, typed with `satisfies SourceRecord`.
    - src/data/defaults.ts exports SEED_WEALTH (value: 200_000, basis: 'real', full SourceRecord) and INFLATION_RATE (value: 0.025, basis: 'nominal', full SourceRecord) as separate frozen constants.
    - npm test -- src/core/__tests__/sourcing.test.ts is GREEN (no regressions).
    - npm run typecheck passes (tsc --noEmit clean on these files).
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend Zustand store with actions and cited seed (ENTRY-01, ENTRY-02, ENTRY-03, ENTRY-04)</name>
  <files>src/state/store.ts</files>
  <read_first>
    - src/state/store.ts — current file (full; it is short, 15 lines)
    - src/data/defaults.ts — SEED_WEALTH and INFLATION_RATE exports just added in Task 2
    - src/state/__tests__/store.test.ts — the stubs from Task 1 that this task must make GREEN
    - src/core/types.ts (Inputs, Params interfaces)
    - 04-PATTERNS.md §"store.ts target shape" — the exact extended interface
  </read_first>
  <action>
    Rewrite src/state/store.ts to add the three actions and the basis field. The new interface is:

    interface ProjectionStore {
      inputs: Inputs;
      params: Params;
      basis: 'real' | 'nominal';
      setInputs:  (patch: Partial<Inputs>) => void;
      setHorizon: (h: number) => void;
      setBasis:   (b: 'real' | 'nominal') => void;
    }

    Import SEED_WEALTH from '../data/defaults.js' (in addition to the existing DEFAULTS import).

    Change the create call from `() => ({...})` to `(set) => ({...})`.

    Replace the hardcoded inputs initializer:
      inputs: { currentWealth: 120_000, annualSavings: 6_000 }
    with:
      inputs: { currentWealth: SEED_WEALTH.value, annualSavings: DEFAULTS.savings.value }

    Add `basis: 'real' as const` to the initial state (D-08 default = real per CONTEXT).

    Add the three setters:
    - setInputs: (patch) => set((s) => ({ inputs: { ...s.inputs, ...patch } }))
    - setHorizon: (h) => set((s) => ({ params: { ...s.params, horizon: h } }))   ← shallow clone of frozen DEFAULTS is safe (D-03 pitfall)
    - setBasis: (b) => set({ basis: b })

    Do NOT import INFLATION_RATE in the store — it is consumed by selectors in Plan 02. Keep the store's responsibility minimal (state + actions only).

    Preserve the existing comment "Phase 4 will add setInputs/setParams actions" by replacing it with "Phase 4: setInputs, setHorizon, setBasis actions added; seed from SEED_WEALTH (D-02/D-03)".
  </action>
  <verify>
    <automated>npm test -- src/state/__tests__/store.test.ts</automated>
  </verify>
  <done>
    - All store test stubs from Task 1 are GREEN.
    - npm test (full suite) is green — no regressions in the sourcing gate or prior selector tests.
    - npm run typecheck passes.
    - store.ts default currentWealth is SEED_WEALTH.value (200_000), not 120_000.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed input → store setInputs | Numeric values from UI fields (not yet built in this plan); clamp guard is Plan 02 concern. Store itself accepts any number — the typed input guard is the imperative shell's responsibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | store.setHorizon | mitigate | Executor must use `{ ...s.params, horizon: h }` (shallow clone), never `s.params.horizon = h`. Frozen DEFAULTS throw in strict mode on in-place mutation. Verified by the store test that confirms DEFAULTS.horizon is unchanged after setHorizon. |
| T-04-01-02 | Tampering | src/data/sources.ts | accept | Static data file; no user input reaches it; no injection sink. Low-value target (no secrets, no PII). |
| T-04-01-SC | Tampering | npm installs | accept | This plan installs zero new packages. No supply-chain risk this wave. |
</threat_model>

<verification>
Full phase gate (run after this plan's tasks complete):
- npm test (full suite green)
- npm run typecheck (tsc --noEmit clean)
- grep -c 'currentWealth: 120_000' src/state/store.ts returns 0 (placeholder removed)
- grep -c 'setInputs\|setHorizon\|setBasis' src/state/store.ts returns ≥3
- grep -c 'scf2022\|blsCpiLongRun' src/data/sources.ts returns ≥2
</verification>

<success_criteria>
- store.test.ts exists and all assertions are GREEN
- SEED_WEALTH.value === 200_000 exported from defaults.ts with a full six-field SourceRecord
- INFLATION_RATE.value === 0.025 exported from defaults.ts with a full six-field SourceRecord (basis: 'nominal')
- store.ts exports useProjectionStore with setInputs, setHorizon, setBasis, and basis: 'real' as the default
- Full test suite green; tsc --noEmit clean
</success_criteria>

<output>
Create `.planning/phases/04-ui-shell-minimal-entry/04-01-SUMMARY.md` when done.
</output>
