---
phase: 05-neutrality-review-release-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/selectors.ts
  - src/ui/AppShell.tsx
  - src/ui/SummaryReadout.tsx
  - src/state/__tests__/selectors.test.ts
autonomous: true
requirements: [NEUT-02]

must_haves:
  truths:
    - "In nominal basis, the Summary Readout rank-delta clause reports the REAL growth multiple (D-14), not the inflated nominal multiple"
    - "The real growth multiple is byte-identical between real and nominal basis for the same projection"
    - "tsc --noEmit passes after the selectSummary signature change (all call sites updated)"
  artifacts:
    - path: "src/state/selectors.ts"
      provides: "Summary.realGrowthMultiple field + two-arg selectSummary(result, rawResult)"
      contains: "realGrowthMultiple"
    - path: "src/state/__tests__/selectors.test.ts"
      provides: "CR-01/D-14 regression test asserting basis-invariant realGrowthMultiple"
      contains: "realGrowthMultiple"
  key_links:
    - from: "src/ui/AppShell.tsx"
      to: "selectSummary(result, rawResult)"
      via: "summary useMemo threading rawResult"
      pattern: "selectSummary\\(result, rawResult\\)"
    - from: "src/ui/SummaryReadout.tsx"
      to: "formatRankDelta"
      via: "summary.realGrowthMultiple argument"
      pattern: "summary\\.realGrowthMultiple"
---

<objective>
Fix the carried mandatory defect CR-01: in nominal basis the Summary Readout
clause "while real wealth grew [G]×" currently passes the re-inflated
(nominal) growth multiple, violating Style Guide §6 / D-14 which requires the
real growth multiple regardless of active basis.

Purpose: CR-01 is a NEUT-02 pre-condition (PROJECT.md Phase 4 completion note).
A shipped surface mislabels a nominal figure as "real" — a hard neutrality/
correctness violation that must be resolved before the review sweep.
Output: `realGrowthMultiple` field on `Summary`, two-arg
`selectSummary(result, rawResult)`, wired through AppShell + SummaryReadout,
with a pure-function regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-neutrality-review-release-readiness/05-RESEARCH.md
@.planning/phases/05-neutrality-review-release-readiness/05-PATTERNS.md
@.planning/phases/04-ui-shell-minimal-entry/04-REVIEW.md
@docs/NEUTRALITY-STYLE-GUIDE.md

<interfaces>
<!-- Extracted from codebase — executor should use these directly. -->

src/state/selectors.ts:400-406 — existing Summary interface:
  endingWealth: number; growthMultiple: number; cagr: number;
  startRank: number; endRank: number;

src/state/selectors.ts:657-671 — selectSummary(r: ProjectionResult): Summary
  uses guard idiom `start > 0 ? end / start : 0`; JSDoc above function.

src/ui/AppShell.tsx:52-58 — rawResult (real-basis engine output) in scope;
  result = selectReinflated(rawResult, basis, ...). AppShell.tsx:96 —
  summary = useMemo(() => result !== null ? selectSummary(result) : null, [result]).

src/ui/SummaryReadout.tsx:52-55 — formatRankDelta(summary.startRank,
  summary.endRank, summary.growthMultiple); props-only component (no store imports).

src/state/__tests__/selectors.test.ts:459-478 — existing real-vs-nominal
  selectSummary invariant tests (the analog template). Uses module-level
  fixture: projectionEngine(syntheticInputs, makeSyntheticParams({horizon:5}))
  and selectReinflated(result, 'nominal', 0.025).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add realGrowthMultiple to Summary + two-arg selectSummary + regression test</name>
  <files>src/state/selectors.ts, src/state/__tests__/selectors.test.ts</files>
  <read_first>
    - src/state/selectors.ts (the Summary interface ~L400-406 and selectSummary ~L657-671 — modify in place, preserve the JSDoc and the `start > 0 ? … : 0` guard idiom)
    - src/state/__tests__/selectors.test.ts (full — the `ENTRY-05: selectSummary` describe block ~L459-478 is the exact analog; note ALL existing selectSummary call sites: L424, L430, L435, L441, L454, L460-477)
    - .planning/phases/05-neutrality-review-release-readiness/05-RESEARCH.md §"CR-01 Deep Dive" and §"Code Examples" (Form (a) is the prescribed shape)
    - .planning/phases/05-neutrality-review-release-readiness/05-PATTERNS.md §"src/state/selectors.ts" and §"src/state/__tests__/selectors.test.ts"
  </read_first>
  <behavior>
    - For a fixture with nonzero inflation: selectSummary over real basis and over selectReinflated(result,'nominal',0.025) yields an IDENTICAL realGrowthMultiple (toBeCloseTo(..., 10)).
    - For the same fixture: nominalSummary.endingWealth > realSummary.endingWealth (nominal is inflated — proves the two metrics are decoupled).
    - growthMultiple remains basis-adjusted (display) and still equals endingWealth/startingWealth of the passed result.
  </behavior>
  <action>
    Add `realGrowthMultiple: number;` to the `Summary` interface (selectors.ts
    ~L400-406) with a `// ALWAYS real-basis — feeds the §6/D-14 clause` comment
    in the existing comment style. Change `selectSummary` to the two-arg form
    `selectSummary(r: ProjectionResult, raw: ProjectionResult): Summary` (RESEARCH
    Form (a)): compute `endingWealth`/`growthMultiple`/`cagr`/ranks from `r` as
    today; compute `realGrowthMultiple` from `raw.series` first/last `userWealth`
    using the same `rStart > 0 ? rEnd / rStart : 0` guard idiom. Extend the
    existing JSDoc block above the function to document the new `raw` arg and
    `realGrowthMultiple` field — do not drop the existing doc. Then add a new
    `it(...)` case INTO the existing `ENTRY-05: selectSummary` describe (after
    ~L478) labelled for CR-01/D-14 asserting the basis-invariant
    realGrowthMultiple per the behavior block. Update EVERY existing
    `selectSummary(...)` call site in selectors.test.ts to the two-arg form
    (pass the real `result` as the second arg where the first arg is `result`;
    pass the original real `result` as `raw` where the first arg is a reinflated
    nominal result, per the §"Code Examples" regression test snippet).
  </action>
  <verify>
    <automated>cd /Users/gama/Documents/personal/assets-projection && npx vitest run src/state/__tests__/selectors.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "realGrowthMultiple" src/state/selectors.ts` shows the interface field AND the computed value in selectSummary.
    - `npx vitest run src/state/__tests__/selectors.test.ts` passes including the new CR-01/D-14 case.
    - The new test asserts `nominal.realGrowthMultiple` toBeCloseTo `real.realGrowthMultiple` (precision 10) AND `nominal.endingWealth` > `real.endingWealth`.
    - No edit to formatRankDelta's template text (RESEARCH Pitfall 3).
  </acceptance_criteria>
  <done>Summary has realGrowthMultiple (always real-basis); selectSummary is two-arg; selectors.test.ts green with the new D-14 regression case; no other call sites broken in that file.</done>
</task>

<task type="auto">
  <name>Task 2: Wire realGrowthMultiple through AppShell + SummaryReadout + full typecheck</name>
  <files>src/ui/AppShell.tsx, src/ui/SummaryReadout.tsx</files>
  <read_first>
    - src/ui/AppShell.tsx (the summary useMemo ~L96 and rawResult scope ~L52-58; note the existing null-guard idiom and `// D-13:` comment convention; relOption/shareOption ~L76-93 already thread rawResult correctly — mirror that)
    - src/ui/SummaryReadout.tsx (the D-14 clause render ~L52-55; component is props-only — preserve that boundary, no store imports)
    - .planning/phases/05-neutrality-review-release-readiness/05-PATTERNS.md §"src/ui/AppShell.tsx" and §"src/ui/SummaryReadout.tsx"
  </read_first>
  <action>
    In `AppShell.tsx` change the `summary` useMemo to call
    `selectSummary(result, rawResult)` guarded on `result !== null &&
    rawResult !== null`, adding `rawResult` to the dependency array — mirror the
    existing relOption/shareOption rawResult-threading idiom; keep the `// D-13:`
    comment. In `SummaryReadout.tsx` change the single token in the
    `formatRankDelta(...)` call from `summary.growthMultiple` to
    `summary.realGrowthMultiple` (the D-14 clause at ~L54). Do NOT modify
    `formatRankDelta` itself. Then run the full typecheck — the `selectSummary`
    signature change must compile across all call sites (RESEARCH Pitfall 4 /
    Assumption A1: only AppShell, SummaryReadout, selectors.test.ts are affected).
  </action>
  <verify>
    <automated>cd /Users/gama/Documents/personal/assets-projection && npm run typecheck && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "selectSummary(result, rawResult)" src/ui/AppShell.tsx` matches.
    - `grep -n "summary.realGrowthMultiple" src/ui/SummaryReadout.tsx` matches; `grep -c "summary.growthMultiple" src/ui/SummaryReadout.tsx` is 0 in the formatRankDelta call (growthMultiple may still appear for the displayed "Growth multiple" metric — confirm only the D-14 clause changed).
    - `npm run typecheck` exits 0 (no unupdated call sites).
    - `npx vitest run` (full suite) passes.
  </acceptance_criteria>
  <done>AppShell threads rawResult into selectSummary; SummaryReadout's D-14 clause uses realGrowthMultiple; tsc --noEmit green; full Vitest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side static SPA; no backend/auth/network/storage. CR-01 is an internal pure-function data-flow correction; introduces no new input crossing a boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | selectSummary signature change breaking call sites silently | mitigate | `npm run typecheck` (tsc --noEmit) is an acceptance criterion in Task 2; all call sites updated in same wave |
| T-05-02 | Information disclosure (correctness/integrity of displayed claim) | Mislabeled nominal multiple as "real wealth" | mitigate | The CR-01 fix itself + pure D-14 regression test asserting basis-invariance (Task 1) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable, no install surface) |
</threat_model>

<verification>
- `npm run typecheck` exits 0.
- `npx vitest run` (full suite) green, including the new CR-01/D-14 case.
- Style Guide §6/D-14 satisfied: the "while real wealth grew [G]×" clause is fed a basis-invariant real multiple.
</verification>

<success_criteria>
- `Summary.realGrowthMultiple` exists and is always real-basis.
- `selectSummary(result, rawResult)` two-arg form used at every call site.
- AppShell + SummaryReadout wired; D-14 clause uses realGrowthMultiple.
- Full Vitest suite + tsc --noEmit green.
</success_criteria>

<output>
Create `.planning/phases/05-neutrality-review-release-readiness/05-01-SUMMARY.md` when done
</output>
