---
phase: 05-neutrality-review-release-readiness
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/ui/CitationFooter.tsx
autonomous: true
requirements: [NEUT-02]

must_haves:
  truths:
    - "A user opening the app sees a neutral survivorship-bias caveat rendered in the sourcing footer (criterion 4 — currently unmet; footer only says 'See sources for definitions and caveats.')"
    - "The caveat text is a traceable condensation of SOURCES.jst2019.note — not invented prose"
    - "The caveat sentence contains no §1-banned verbs/adjectives/framing and no alarm punctuation"
  artifacts:
    - path: "src/ui/CitationFooter.tsx"
      provides: "Rendered JST survivorship caveat sentence as a second <p> in the existing <footer>"
      contains: "survivorship"
  key_links:
    - from: "src/ui/CitationFooter.tsx"
      to: "SOURCES.jst2019.note (sources.ts:173-182)"
      via: "neutral condensation of the cited survivorship sentence"
      pattern: "0\\.5pp|survivorship"
---

<objective>
Surface the long-run-historical (JST) survivorship caveat neutrally in the
sourcing affordance (ROADMAP Phase 5 criterion 4). The caveat already exists
in `SOURCES.jst2019.note` (`src/data/sources.ts:173-182`) but is never
rendered — `CitationFooter` currently only emits the static pointer
"See sources for definitions and caveats."

Purpose: criterion 4 is genuinely unmet today; a released release-ready tool
must surface the cited caveat for its survivorship-biased long-run anchor.
Output: a §1-compliant single sentence, traceable to `jst2019.note`, rendered
as a second `<p>` in the existing `CitationFooter` `<footer>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-neutrality-review-release-readiness/05-RESEARCH.md
@.planning/phases/05-neutrality-review-release-readiness/05-PATTERNS.md
@docs/NEUTRALITY-STYLE-GUIDE.md
@src/ui/CitationFooter.tsx
@src/data/sources.ts

<interfaces>
<!-- Extracted from codebase. -->

src/ui/CitationFooter.tsx:3-35 — props-only component:
  CitationFooter({ citations }: Props) → single <footer
  className="mt-12 text-sm text-slate-600 font-normal border-t border-slate-200 pt-6">
  with one <p> mapping citations + trailing "See sources for definitions and caveats."
  NO store/SOURCES import inside the component (props-only boundary —
  PATTERNS §"Props-only UI component boundary").

src/data/sources.ts:173-182 — SOURCES.jst2019.note (read-only source of truth):
  "Survivorship bias (Pitfall 7): JST Appendix sensitivity tests indicate
   ~0.5pp upward bias in reported real equity/housing returns from restricting
   to markets that survived without revolution/expropriation. Conservative
   calibration should shade returns ~0.5pp below JST headlines; the PROJECT.md
   indicative band lower ends reflect this adjustment. …"

docs/NEUTRALITY-STYLE-GUIDE.md §1 — banned verbs/adjectives/framing + no alarm
  punctuation. The new sentence is itself a NEUT-02 row reviewed against §1 in Plan 03.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render a sourced, §1-compliant JST survivorship caveat in CitationFooter</name>
  <files>src/ui/CitationFooter.tsx</files>
  <read_first>
    - src/ui/CitationFooter.tsx (full — extend the existing <footer>, add a second <p> in the same `text-sm text-slate-600` class family; do NOT introduce a new component or import SOURCES into the component — keep props-only)
    - src/data/sources.ts:155-185 (the jst2019 SourceRecord — the caveat text MUST be a traceable condensation of the `note` field; CLAUDE.md data-integrity: surface the cited caveat, do not invent one)
    - docs/NEUTRALITY-STYLE-GUIDE.md §1 (Copy Lexicon — banned verbs/adjectives/framing, no alarm punctuation; no "unreliable"/"flawed")
    - .planning/phases/05-neutrality-review-release-readiness/05-RESEARCH.md §"Survivorship Caveat Surfacing" (Option A recommended) and 05-PATTERNS.md §"src/ui/CitationFooter.tsx"
  </read_first>
  <action>
    Add a second `<p>` inside the existing `CitationFooter` `<footer>` (reuse
    the existing `text-sm text-slate-600` Tailwind class family for visual
    consistency; no new component, no SOURCES import — keep the component
    props-only). Render a single factual sentence that is a neutral
    condensation of `SOURCES.jst2019.note` (sources.ts:173-182): it must state
    that the long-run historical return figures carry an estimated ~0.5pp
    upward survivorship bias (from restricting to markets that survived without
    expropriation) and that the defaults are shaded conservatively below the
    headline figures. Obey §1: no banned verbs/adjectives/framing, no alarm
    punctuation ("!"), no "unreliable"/"flawed"/"biased data" editorializing —
    state the mechanism as fact. Per RESEARCH Option A scope JST only (the
    primary survivorship-biased default); do not enumerate every note-bearing
    source. The exact wording is executor discretion but every factual claim
    (~0.5pp, market survival, conservative shading) must be traceable to the
    jst2019 note string. If the condensed string is derived rather than
    inlined, derive it upstream and pass via props — do NOT import SOURCES into
    the component (PATTERNS props-only boundary). Scope = JST only this plan
    (per RESEARCH Assumption A2 / Option A); fagereng2020.note is secondary and
    out of criterion-4 literal scope.
  </action>
  <verify>
    <automated>cd /Users/gama/Documents/personal/assets-projection && grep -niE "survivorship|0\.5pp" src/ui/CitationFooter.tsx && grep -c '!' src/ui/CitationFooter.tsx | grep -qx 0 && npm run typecheck && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -niE "survivorship|0\.5pp" src/ui/CitationFooter.tsx` matches the new sentence.
    - The sentence's factual claims are present in `src/data/sources.ts` jst2019.note (manual traceability check during execution — record the source span in the SUMMARY).
    - No "!" alarm punctuation in CitationFooter.tsx; no banned §1 verbs/adjectives ("unreliable", "flawed", "shocking", etc.) in the new sentence.
    - Component remains props-only — no `import { SOURCES }` (or equivalent) added to CitationFooter.tsx.
    - `npm run typecheck` exits 0; `npx vitest run` (full suite) green.
  </acceptance_criteria>
  <done>The footer renders a sourced, §1-compliant survivorship caveat traceable to jst2019.note; props-only boundary preserved; typecheck + full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static SPA. The caveat string is a static constant traceable to sources.ts (not user-derived); no new input boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering / Repudiation (data integrity) | Inventing un-sourced caveat prose | mitigate | Acceptance criterion: every factual claim traceable to jst2019.note; CLAUDE.md data-integrity rule enforced in read_first + verify |
| T-05-04 | Tampering | Caption/footer HTML injection | accept | Caveat is a static string constant, not user input (RESEARCH §Security Domain — already-mitigated pattern) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- The footer visibly renders a survivorship caveat (criterion 4 met).
- Caveat is traceable to `SOURCES.jst2019.note`; no invented claims.
- §1-compliant (no banned terms, no alarm punctuation).
- `npm run typecheck` + `npx vitest run` green.
</verification>

<success_criteria>
- CitationFooter renders a sourced, neutral JST survivorship caveat.
- Component stays props-only (no SOURCES import).
- New sentence is ready to be reviewed as its own NEUT-02 row in Plan 03.
</success_criteria>

<output>
Create `.planning/phases/05-neutrality-review-release-readiness/05-02-SUMMARY.md` when done
</output>
