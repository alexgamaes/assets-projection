---
phase: 05-neutrality-review-release-readiness
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - .planning/phases/05-neutrality-review-release-readiness/05-NEUT-02-REVIEW.md
  - src/state/__tests__/selectors.test.ts
  - docs/NEUTRALITY-STYLE-GUIDE.md
autonomous: true
requirements: [NEUT-02]

must_haves:
  truths:
    - "Every user-facing string and palette constant across the 16 swept source files has a recorded PASS/FIXED verdict citing the specific style-guide rule (D-01/D-02/D-03)"
    - "The four seeded verbatim captions (§3/§5/§6/§7) are byte-exact equality-asserted in Vitest"
    - "Phase 5 exits with ZERO open FAIL rows — every FAIL is fixed inline and flipped to FIXED with a fix-ref (D-04)"
    - "The §3 HarnessPage.tsx→AppShell.tsx style-guide drift is resolved (doc corrected + version bump) or explicitly recorded as a frozen-guide living-doc finding"
  artifacts:
    - path: ".planning/phases/05-neutrality-review-release-readiness/05-NEUT-02-REVIEW.md"
      provides: "Exhaustive surface-grouped per-item review with zero open FAIL rows"
      contains: "open FAIL: 0"
    - path: "src/state/__tests__/selectors.test.ts"
      provides: "Byte-exact equality assertions for §3 REL_POS_CAPTION and §7 SHARE_CAPTION"
      contains: "REL_POS_CAPTION"
  key_links:
    - from: "05-NEUT-02-REVIEW.md"
      to: "docs/NEUTRALITY-STYLE-GUIDE.md §1–§7"
      via: "every row cites the specific section/rule checked against"
      pattern: "§[1-7]"
    - from: "src/state/__tests__/selectors.test.ts"
      to: "REL_POS_CAPTION / SHARE_CAPTION"
      via: "expect(CONST).toBe(verbatim style-guide text)"
      pattern: "\\.toBe\\("
---

<objective>
Produce the deliverable of record: `05-NEUT-02-REVIEW.md` — an exhaustive,
surface-grouped, per-item neutrality review of every shipped user-facing
string and palette constant (D-01/D-02/D-03), with byte-exact equality
assertions for the four seeded verbatim captions, every FAIL fixed inline and
flipped to FIXED (D-04), and the §3 style-guide drift resolved. Phase 5 exits
with zero open FAIL rows.

Purpose: this IS the NEUT-02 release gate. "Release readiness" =
no shipped neutrality violation remains.
Output: the review artifact, new caption-equality Vitest assertions, any
FAIL→FIXED string corrections, and an optional style-guide version bump.

Note: reviews the OUTPUT of Plans 01 (CR-01 fix → that row is FIXED) and 02
(new survivorship caveat → reviewed as its own row vs §1) — hence wave 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-neutrality-review-release-readiness/05-CONTEXT.md
@.planning/phases/05-neutrality-review-release-readiness/05-RESEARCH.md
@.planning/phases/05-neutrality-review-release-readiness/05-PATTERNS.md
@.planning/phases/05-neutrality-review-release-readiness/05-01-SUMMARY.md
@.planning/phases/05-neutrality-review-release-readiness/05-02-SUMMARY.md
@docs/NEUTRALITY-STYLE-GUIDE.md

<interfaces>
<!-- The closed 16-file surface inventory (RESEARCH §Pattern 1 — this IS the
exhaustive list; no other files render user-facing copy). -->

src/ui/* (10): AppShell.tsx, SummaryReadout.tsx, summaryFormatters.ts,
  CitationFooter.tsx, ControlPanel.tsx, BasisToggle.tsx, HorizonSlider.tsx,
  LogLinearToggle.tsx, LogSliderInput.tsx, __tests__/summaryFormatters.test.ts (excluded — test-only)
src/viz/* (5): TimeSeriesChart.tsx (no literals), DivergenceChart.tsx (no literals),
  RelativePosChart.tsx (caption prop only), TierShareChart.tsx (caption prop only),
  DonutChart.tsx (caption prop only)
src/state/selectors.ts (1): COLORS palette (L13-20 §4), axis/series/tooltip
  strings, deriveTier labels, markLine labels, donut center text (L602-605),
  SHARE_CAPTION (L425-426 §7)

Verbatim caption canonical text + shipped locations:
  §3 REL_POS_CAPTION  — AppShell.tsx:32-33   — style guide §3 block
  §5 nominal caption  — summaryFormatters.ts:52-54 (formatMoneyIllusionCaption) — §5 template
  §6 D-14 clause      — summaryFormatters.ts:30-32 (formatRankDelta) — §6 template
  §6 D-15 disclosure  — SummaryReadout.tsx:58-61 (JSX, &apos; → ') — §6 block
  §7 SHARE_CAPTION    — selectors.ts:425-426  — §7 block (byte-exact by inspection)

selectors.test.ts:625-636 — established expect(CONST).toBe("…") byte-exact pattern.
RESEARCH §"Review Artifact Schema" — the canonical markdown skeleton (use verbatim).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verbatim-caption byte-exact equality assertions (§3 + §7) + §5/§6 confirmation</name>
  <files>src/state/__tests__/selectors.test.ts</files>
  <read_first>
    - src/state/__tests__/selectors.test.ts:625-636 (the established `describe(...) → expect(CONST).toBe("…")` byte-exact precedent — mirror this shape)
    - src/state/selectors.ts (SHARE_CAPTION ~L425-426 and the import surface; REL_POS_CAPTION lives in AppShell.tsx:32-33)
    - src/ui/AppShell.tsx:32-33 (REL_POS_CAPTION constant — confirm it is exported/importable; if not exported, the equality check is a source-read row in Task 2 instead)
    - src/ui/__tests__/summaryFormatters.test.ts (existing §5/§6 template assertions — confirm coverage, tighten only if a gap exists)
    - docs/NEUTRALITY-STYLE-GUIDE.md §3 and §7 (the canonical verbatim text to paste as the expected string)
    - .planning/phases/05-neutrality-review-release-readiness/05-PATTERNS.md §"Verbatim-caption byte-exact equality"
  </read_first>
  <action>
    Add Vitest equality assertions mirroring the selectors.test.ts:625-636
    `expect(CONST).toBe("<verbatim style-guide text>")` pattern: (1) assert
    `SHARE_CAPTION` byte-equals the §7 canonical caption text; (2) assert
    `REL_POS_CAPTION` byte-equals the §3 canonical caption text — import it
    from AppShell if exported; if it is not module-exported, do NOT add a DOM
    test (no DOM tooling — RESEARCH Anti-Pattern) — instead record §3 as a
    source-read equality row in Task 2's artifact and note this here. Confirm
    `summaryFormatters.test.ts` already asserts the §5 nominal template and §6
    D-14 clause template shape; if either lacks an exact-template assertion,
    add one (the §6 D-15 disclosure is JSX with `&apos;` — normalize
    `&apos;`→`'` before comparison per RESEARCH Pitfall 1; if it can only be
    checked by source-read because it is inline JSX, record it as a source-read
    equality row in Task 2). Paste the style-guide text exactly — no fuzzy/
    normalized matching (RESEARCH Don't Hand-Roll).
  </action>
  <verify>
    <automated>cd /Users/gama/Documents/personal/assets-projection && npx vitest run && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SHARE_CAPTION" src/state/__tests__/selectors.test.ts` shows a `.toBe(` byte-exact assertion against the §7 text.
    - §3 REL_POS_CAPTION either has a `.toBe(` assertion (if exported) OR is explicitly recorded as a source-read equality row in Task 2 (no DOM test added).
    - §5 + §6 templates have exact assertions (existing or newly added) in summaryFormatters.test.ts; the §6 D-15 disclosure comparison normalizes `&apos;`→`'`.
    - `npx vitest run` (full suite) green; `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The deterministic verbatim-caption rows are machine-asserted (or explicitly source-read-scoped where no module export exists); full suite + typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Exhaustive surface sweep → 05-NEUT-02-REVIEW.md, FAIL→FIXED inline, §3 doc drift resolution</name>
  <files>.planning/phases/05-neutrality-review-release-readiness/05-NEUT-02-REVIEW.md, docs/NEUTRALITY-STYLE-GUIDE.md, (any src/ui|src/viz|src/state file owning a FAIL literal)</files>
  <read_first>
    - .planning/phases/05-neutrality-review-release-readiness/05-RESEARCH.md §"Pattern 1" (the auditable 16-file inventory table — every literal + the §rule to check it against), §"Review Artifact Schema" (use the markdown skeleton verbatim), §"Common Pitfalls" (1–5)
    - docs/NEUTRALITY-STYLE-GUIDE.md §1–§7 (every row must cite the specific section/rule it was checked against)
    - All 16 surface files listed in the RESEARCH inventory: src/ui/AppShell.tsx, SummaryReadout.tsx, summaryFormatters.ts, CitationFooter.tsx (NOW includes the Plan-02 caveat — review it vs §1), ControlPanel.tsx, BasisToggle.tsx, HorizonSlider.tsx, LogLinearToggle.tsx, LogSliderInput.tsx; src/viz/TimeSeriesChart.tsx, DivergenceChart.tsx, RelativePosChart.tsx, TierShareChart.tsx, DonutChart.tsx; src/state/selectors.ts
    - .planning/phases/05-neutrality-review-release-readiness/05-01-SUMMARY.md and 05-02-SUMMARY.md (the CR-01 row is FIXED with that commit/diff ref; the new caveat string is a row reviewed vs §1)
    - .planning/phases/05-neutrality-review-release-readiness/05-CONTEXT.md (D-01/D-02/D-03/D-04 + living-doc discretion)
  </read_first>
  <action>
    Run the completeness-evidence sweep
    `grep -rnoE '"[^"]{2,}"|`[^`]{2,}`' src/ui src/viz src/state/selectors.ts
    --include='*.ts' --include='*.tsx' | grep -v '__tests__'` and embed its
    evidence in the artifact. Create
    `05-NEUT-02-REVIEW.md` using the RESEARCH §"Review Artifact Schema"
    skeleton verbatim (columns: # · string/literal · location · style-guide
    rule · verdict PASS/FAIL/FIXED · rationale · fix-ref), surface-grouped,
    enumerating EVERY user-facing literal across all 16 files — including the
    two literal-free viz wrappers marked "none — verified" and the excluded
    __tests__ note (D-02 auditability; RESEARCH Pitfall 5: never sample).
    Each row cites the specific §-rule (§1 lexicon / §2 chart-semantic / §3 /
    §4 palette / §5 / §6 / §7). Record the CR-01 row as FIXED with the Plan-01
    commit/diff fix-ref. Review the new Plan-02 survivorship caveat as its own
    row vs §1. Include the four verbatim-caption rows (cross-reference Task 1's
    machine assertions; for §3/§6-disclosure use source-read equality, §apos
    normalized). For EVERY row that is FAIL: fix the offending literal inline
    in its owning file, flip the row to FIXED, record the fix-ref (commit/diff
    or file:line of the correction), re-verify to PASS — Phase 5 exits with
    ZERO open FAIL rows (D-04). For the §3 HarnessPage.tsx→AppShell.tsx style-
    guide drift (RESEARCH Pitfall 2 / State of the Art): apply the living-doc
    default lean — correct the §3 reference to `AppShell.tsx` `REL_POS_CAPTION`,
    bump the style guide version (header `**Version:**`), and record the
    version reviewed-against in the artifact header; if a FAIL exposes a gap no
    existing rule names, add the rule + version-bump per the same policy. End
    the artifact with the explicit `Total rows: N · PASS: x · FIXED: y ·
    open FAIL: 0` summary line. Do NOT edit a verbatim style-guide caption to
    retro-fit a non-conforming shipped string — the shipped string is the FAIL
    and gets fixed (RESEARCH Anti-Pattern).
  </action>
  <verify>
    <automated>cd /Users/gama/Documents/personal/assets-projection && test -f .planning/phases/05-neutrality-review-release-readiness/05-NEUT-02-REVIEW.md && grep -qE 'open FAIL:?\s*0' .planning/phases/05-neutrality-review-release-readiness/05-NEUT-02-REVIEW.md && ! grep -nE '^\|.*\|\s*FAIL\s*\|' .planning/phases/05-neutrality-review-release-readiness/05-NEUT-02-REVIEW.md && npx vitest run && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `05-NEUT-02-REVIEW.md` exists, surface-grouped, with a row for every user-facing literal across all 16 files (the two literal-free viz wrappers explicitly marked "none — verified"; __tests__ explicitly excluded with a note).
    - Embedded grep-completeness evidence command + output is present (auditability, D-02).
    - Every row cites a specific style-guide §-rule; no row has an open `FAIL` verdict (only PASS or FIXED); FIXED rows carry a fix-ref.
    - The CR-01 row is present and FIXED (Plan-01 ref); the Plan-02 survivorship caveat is present and reviewed vs §1.
    - Style guide §3 `HarnessPage.tsx` reference corrected to `AppShell.tsx`/`REL_POS_CAPTION` and `**Version:**` bumped; the reviewed-against version recorded in the artifact header.
    - Final summary line shows `open FAIL: 0`.
    - `npx vitest run` (full suite) + `npm run typecheck` green after any inline FAIL→FIXED string corrections.
  </acceptance_criteria>
  <done>Exhaustive, auditable 05-NEUT-02-REVIEW.md with zero open FAIL rows; all FAILs fixed inline + flipped to FIXED with fix-refs; §3 drift resolved + guide version-bumped; suite + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation + static-string review of an already-shipped client SPA. No backend/auth/network/storage; no new input boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Repudiation (audit integrity) | Sampled/incomplete inventory presented as exhaustive | mitigate | D-02: embed grep-completeness evidence; enumerate all 16 files incl. literal-free wrappers; acceptance criterion checks for it |
| T-05-06 | Tampering (contract integrity) | Editing a verbatim style-guide caption to mask a non-conforming shipped string | mitigate | Action + acceptance forbid retro-fitting the guide; shipped string is the FAIL and gets fixed; version-bump only to ADD a rule / fix the §3 drift |
| T-05-07 | Tampering | Inline FAIL→FIXED string edit breaking the build/tests | mitigate | `npx vitest run` + `npm run typecheck` in verify and acceptance after corrections |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `05-NEUT-02-REVIEW.md` exists, exhaustive (16 files), surface-grouped, every row rule-cited.
- Zero open FAIL rows; FIXED rows carry fix-refs; CR-01 + survivorship-caveat rows present.
- §3 doc drift corrected + style guide version bumped.
- Verbatim captions byte-asserted (Task 1) or source-read-equality recorded.
- `npx vitest run` + `npm run typecheck` green.
</verification>

<success_criteria>
- NEUT-02 satisfied: recorded pass/fail per item, no value-laden language/alarm punctuation/semantic red-green remaining, rel-position no-zero-sum caption verified, survivorship caveat reviewed.
- Phase 5 exits with zero open FAIL rows (release-ready).
</success_criteria>

<output>
Create `.planning/phases/05-neutrality-review-release-readiness/05-03-SUMMARY.md` when done
</output>
