---
phase: 01-model-foundation
plan: 03
type: tdd
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/core/tiers.ts
  - src/core/drag.ts
  - src/core/relativePosition.ts
  - src/core/engine.ts
  - src/core/__tests__/goldenMaster.test.ts
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03, MODEL-04, MODEL-06]

must_haves:
  truths:
    - "projectionEngine(inputs, params) returns a year-by-year ProjectionResult over the horizon (≤60) with zero framework/DOM/fetch imports in core/"
    - "Each anchor tier compounds with the end-of-year ordinary-annuity convention W' = W·(1+r_eff) + S (S real, D-08)"
    - "Drag is a single scalar real-return haircut: assetInflation = dragStrength × dynamic-top-set growth rate, applied once per tier per year (D-01/D-02)"
    - "The engine projects all 4 tiers (not just the user line) and re-reads the user's percentile off the (endogenously re-fit) curve each year so the user's return follows their moving tier (MODEL-02/MODEL-03/D-06)"
    - "With dragStrength=0 the engine output matches the analytic ordinary-annuity closed form within relErr < 1e-9 over the 60-year max horizon (the walking-skeleton golden master)"
  artifacts:
    - path: "src/core/engine.ts"
      provides: "projectionEngine annual loop: dynamic top set → scalar drag → ordinary-annuity step → re-fit → moving-tier read → snapshot"
      exports: ["projectionEngine"]
      min_lines: 60
    - path: "src/core/drag.ts"
      provides: "Scalar asset-price-inflation haircut from dynamic-top-set growth (no transfer function)"
      exports: ["assetInflationFromTopGrowth"]
    - path: "src/core/tiers.ts"
      provides: "Per-tier ordinary-annuity compounding step"
      exports: ["stepAnchorWealth"]
    - path: "src/core/relativePosition.ts"
      provides: "Wealth-share / rank derivation from the curve series"
      exports: ["deriveShares"]
    - path: "src/core/__tests__/goldenMaster.test.ts"
      provides: "Drag-off single-tier vs analytic annuity, <1e-9 over 60y"
      contains: "1e-9"
  key_links:
    - from: "src/core/engine.ts"
      to: "src/core/distribution.ts"
      via: "calibrateCurve / dynamicTopSetPercentile / percentileOf / returnAtPercentile"
      pattern: "from ['\"]\\./distribution"
    - from: "src/core/engine.ts"
      to: "src/core/drag.ts"
      via: "assetInflationFromTopGrowth"
      pattern: "from ['\"]\\./drag"
    - from: "src/core/__tests__/goldenMaster.test.ts"
      to: "src/core/engine.ts"
      via: "projectionEngine"
      pattern: "from ['\"]\\.\\./engine"
---

<objective>
Build the engine vertical slice: the thinnest real end-to-end `(inputs, params) → ProjectionResult` path — per-tier ordinary-annuity compounding, the scalar drag coupling, the annual loop with endogenous re-fit and moving-tier read, relative-position derivation — proven by the drag-off single-tier golden master matching the analytic annuity to <1e-9 over 60 years. This IS the Walking Skeleton capability for a library-only phase: the smallest verified working engine.

Purpose: Deliver a trustworthy, end-to-end-runnable engine. After this plan a real consumer can call `projectionEngine` and get a numerically-correct multi-year per-tier projection.
Output: `tiers.ts`, `drag.ts`, `relativePosition.ts`, `engine.ts`, green golden master.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-model-foundation/01-CONTEXT.md
@.planning/phases/01-model-foundation/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@src/core/types.ts
@src/core/distribution.ts
@src/core/__tests__/testUtils.ts

<interfaces>
<!-- Contracts this plan DEFINES for Plan 04 (invariant battery). -->

src/core/tiers.ts: `stepAnchorWealth(anchorWealth, returnByTier, assetInflation:number, savings:number):AnchorWealth` — for each anchor: `r_eff = realReturn − assetInflation`; `W' = W·(1+r_eff) + S` (end-of-year ordinary annuity, Claude's Discretion convention; S is real, D-08). Drag is `small − small` (never a difference of large compounded balances — Pitfall 10).
src/core/drag.ts: `assetInflationFromTopGrowth(topSetGrowthRate:number, dragStrength:number):number` = `dragStrength * topSetGrowthRate` (D-02; linear; dragStrength=0 ⇒ 0). NO function in this file moves wealth between tiers (Anti-Pattern 3 / Pitfall 4 — code-review invariant).
src/core/relativePosition.ts: `deriveShares(series:YearSnapshot[]):Array<{year:number;userShare:number;userRank:number}>` — derived from the curve's cumulative-share integral; userRank from `percentileOf`.
src/core/engine.ts: `projectionEngine(inputs:Inputs, params:Params):ProjectionResult` — the loop in RESEARCH Pattern 1, adapted: assert real basis at the boundary (`assertReal` on each rate-bearing param — wire so a nominal param throws, satisfying Plan 04's basis.test.ts); year 0 calibrate; for year 0..horizon: dynamicTopSetPercentile → top-set growth rate → assetInflationFromTopGrowth → stepAnchorWealth → re-fit curve (`endogenous` default per D-06 / `fixed-shape-scaled` shift-scale path per D-07, both implemented) → re-read user percentile (MODEL-02 moving tier) → snapshot. User is a tracked test-particle that reads the curve but does NOT perturb anchors (RESEARCH Assumption A5 / CONTEXT deferred — confirmed: negligible single-saver mass).
</interfaces>
</context>

<feature>
  <name>projectionEngine: scalar-drag annual loop with golden-master correctness</name>
  <files>src/core/tiers.ts, src/core/drag.ts, src/core/relativePosition.ts, src/core/engine.ts, src/core/__tests__/goldenMaster.test.ts</files>
  <read_first>
    - .planning/phases/01-model-foundation/01-RESEARCH.md (§"Pattern 1", §"Pattern 4 endogenous vs fixed-shape", §"Code Examples" golden master, §"Don't Hand-Roll" — golden master MUST be independent closed form, §"Open Question 3" user-as-test-particle)
    - .planning/phases/01-model-foundation/01-CONTEXT.md (D-01/D-02 scalar haircut once, D-06/D-07 evolution switch, D-08 real savings, D-09 horizon ≤60, D-10/D-11 golden master + <1e-9, Claude's Discretion end-of-year annuity)
    - .planning/research/PITFALLS.md (Pitfall 2 drag double-count, Pitfall 3 zero-sum, Pitfall 4 floating-point, Anti-Patterns)
    - .planning/research/ARCHITECTURE.md (§"Tier-Coupling / Drag Data Flow", Anti-Pattern 3 finite-pie)
    - src/core/distribution.ts (calibrateCurve/dynamicTopSetPercentile/percentileOf/returnAtPercentile contracts)
    - src/core/types.ts, src/core/__tests__/testUtils.ts (analyticOrdinaryAnnuity, relErr, makeSyntheticParams)
  </read_first>
  <behavior>
    - `goldenMaster.test.ts`: with `makeSyntheticParams({dragStrength:0, horizon:60})` reduced to a single tier, `relErr(engineFinalWealth, analyticOrdinaryAnnuity(W0,r,S,60)) < 1e-9`. Also assert intermediate years (e.g. year 1, 30) match the closed form < 1e-9 (FP-stability spot check; full 60y stability is Plan 04).
    - With dragStrength=0 across all 4 tiers, each tier's terminal wealth matches its independent `analyticOrdinaryAnnuity` within relErr<1e-9 (exact collapse to baseline — Pitfall 3 guard; full D-12 battery in Plan 04).
    - `assetInflationFromTopGrowth(g,0) === 0` and `=== 0.3*g` for dragStrength 0.3 (linear, D-02).
    - Engine returns `series.length === horizon+1`; each snapshot carries anchorWealth for all 4 tiers, userWealth, userPercentile, topSetPercentile, assetInflation (MODEL-03 full distribution).
    - User percentile is re-read each year off the re-fit curve; with heterogeneous returns the user's applied rate changes as their percentile moves (MODEL-02) — assert userPercentile differs between year 0 and year 60 under divergent returns.
    - Feeding a `returnByTier` param tagged `basis:'nominal'` causes `projectionEngine` to throw `/Basis violation/` (wires MODEL-05 for Plan 04's basis.test.ts).
  </behavior>
  <implementation>
    RED: replace `goldenMaster.test.ts` Plan-01 todos with the assertions above. Run — fails (engine absent).
    GREEN: implement `tiers.ts` (ordinary-annuity step, r_eff = r − assetInflation structured as small−small), `drag.ts` (scalar `dragStrength*topGrowth`, zero transfer function — assert by construction), `relativePosition.ts` (`deriveShares` from the curve share integral + percentileOf rank), `engine.ts` (Pattern 1 loop: assertReal at boundary; year-0 calibrate; per-year top-set→drag→step→re-fit(endogenous default, fixed-shape-scaled path present)→moving-tier read→snapshot; user reads curve, does not mutate anchors). `analyticOrdinaryAnnuity` stays the independent reference from testUtils — engine code MUST NOT be imported by it. All modules `core/`-pure.
    REFACTOR: keep the loop readable; ensure no quantity is conserved across tiers (grep/code-review invariant: no subtraction of one tier's gain from another's balance — Pitfall 4); keep Plan 01 import-boundary + Plan 02 distribution tests green.
  </implementation>
</feature>

<verify>
<automated>npx vitest run src/core/__tests__/goldenMaster.test.ts src/core/__tests__/distribution.test.ts src/core/__tests__/invariants.test.ts && npx tsc --noEmit</automated>
</verify>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (in-engine) | NONE — pure offline numerical engine; params injected, no I/O/network/untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-FP | Denial / (correctness) | 60y compounding + drag subtraction | mitigate | Golden master relErr<1e-9 vs independent closed form; drag as small−small not large−large (Pitfall 10) |
| T-03-ZS | Tampering (model integrity) | drag.ts | mitigate | No transfer function by construction; non-conservation invariant (full assertion Plan 04); code-review checklist |
| T-03-BASIS | Tampering (data integrity) | engine boundary | mitigate | assertReal wired at boundary; nominal param throws (MODEL-05, asserted Plan 04) |
| T-03-DET | (correctness, not security) | core/ purity | mitigate | Inherits Plan 01 import-scan; no Date.now/Math.random/fetch/DOM added |

No auth/session/crypto/network/web threats — framework-free in-process math, null external surface (RESEARCH §"Security Domain").
</threat_model>

<verification>
- `npx vitest run src/core/__tests__/goldenMaster.test.ts` green; drag-off single-tier relErr < 1e-9 over 60y (MODEL-06/D-11).
- Engine projects all 4 tiers + user line; user percentile moves year 0→60 (MODEL-02/MODEL-03).
- Plan 01 import-boundary + Plan 02 distribution suites still green (core/ purity, no regression).
- `grep` confirms no transfer/redistribution in drag.ts (Pitfall 4).
</verification>

<success_criteria>
A real, deterministic, framework-free `projectionEngine` produces a year-by-year per-tier projection with the scalar drag and moving-tier returns, proven correct against an independent closed-form reference to <1e-9 — the engine walking skeleton works end to end.
</success_criteria>

<output>
Create `.planning/phases/01-model-foundation/01-03-SUMMARY.md` when done.
</output>
