---
phase: 01-model-foundation
plan: 04
type: tdd
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/core/__tests__/invariants.test.ts
  - src/core/__tests__/basis.test.ts
  - src/core/__tests__/numericalStability.test.ts
  - src/core/__tests__/multiTierFixture.test.ts
autonomous: true
requirements: [MODEL-04, MODEL-05, MODEL-06]

must_haves:
  truths:
    - "A test confirms dragStrength=0 collapses the full engine to independent per-tier ordinary-annuity baseline within relErr<1e-9 (D-12, MODEL-04)"
    - "A test confirms aggregate real wealth can grow unbounded and is NOT conserved as drag increases (non-conservation; no zero-sum transfer; MODEL-04)"
    - "A test confirms no tier is forced to negative real wealth by drag alone (infinite-growth preserved; MODEL-04)"
    - "A test confirms a nominal-basis param consumed where real is expected fails (MODEL-05)"
    - "A test confirms 60-year floating-point stability vs the closed-form reference within relErr<1e-9 (MODEL-06)"
    - "A hand-derived 2-3 tier, ~5-year, drag-on fixture (including re-fit + dynamic top set) matches engine output (D-10b, MODEL-03/MODEL-04)"
  artifacts:
    - path: "src/core/__tests__/invariants.test.ts"
      provides: "drag=0 baseline, non-conservation, infinite-growth, monotone-divergence; plus the existing import-boundary test"
      contains: "dragStrength"
    - path: "src/core/__tests__/basis.test.ts"
      provides: "End-to-end basis-mismatch through projectionEngine throws"
      contains: "Basis violation"
    - path: "src/core/__tests__/numericalStability.test.ts"
      provides: "60y FP-stability vs closed form within relErr<1e-9"
      contains: "1e-9"
    - path: "src/core/__tests__/multiTierFixture.test.ts"
      provides: "Hand-derived 2-3 tier ~5y drag-on golden fixture for the coupling pipeline"
      contains: "projectionEngine"
  key_links:
    - from: "src/core/__tests__/invariants.test.ts"
      to: "src/core/engine.ts"
      via: "projectionEngine + analyticOrdinaryAnnuity"
      pattern: "from ['\"]\\.\\./engine"
    - from: "src/core/__tests__/multiTierFixture.test.ts"
      to: "src/core/engine.ts"
      via: "projectionEngine vs hand-derived expected values"
      pattern: "from ['\"]\\.\\./engine"
---

<objective>
Complete the D-12 invariant battery — the test suite is as much the deliverable as the engine. Fill every remaining Wave-0 placeholder with real assertions: drag=0 baseline collapse, non-conservation / no zero-sum, infinite-growth preserved, monotone divergence, end-to-end basis-mismatch failure, 60-year floating-point stability, and the hand-derived multi-tier drag-on fixture for the coupling pipeline.

Purpose: Prove the four highest-severity pitfalls (P1 nominal/real, P3 drag double-count, P4 zero-sum, P10 FP) are mitigated, satisfying ROADMAP Phase 1 success criteria 3, 4, 5.
Output: Fully green `npx vitest run --coverage`; no `it.todo` remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-model-foundation/01-CONTEXT.md
@.planning/phases/01-model-foundation/01-RESEARCH.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@src/core/types.ts
@src/core/engine.ts
@src/core/drag.ts
@src/core/tiers.ts
@src/core/__tests__/testUtils.ts
</context>

<feature>
  <name>D-12 invariant + golden-fixture battery</name>
  <files>src/core/__tests__/invariants.test.ts, src/core/__tests__/basis.test.ts, src/core/__tests__/numericalStability.test.ts, src/core/__tests__/multiTierFixture.test.ts</files>
  <read_first>
    - .planning/phases/01-model-foundation/01-CONTEXT.md (D-10 golden-master both forms, D-11 <1e-9, D-12 required invariant list)
    - .planning/phases/01-model-foundation/01-RESEARCH.md (§"Code Examples" — invariant test sketches; §"Phase Requirements → Test Map"; §"Don't Hand-Roll" — independent reference)
    - .planning/research/PITFALLS.md (Pitfall 1/3/4/10 + "Looks Done But Isn't" checklist drag-mechanism row)
    - src/core/engine.ts, src/core/drag.ts, src/core/tiers.ts (behavior under test)
    - src/core/__tests__/testUtils.ts (analyticOrdinaryAnnuity, relErr, makeSyntheticParams)
  </read_first>
  <behavior>
    - invariants.test.ts (replace remaining todos; KEEP the existing green import-boundary/determinism test from Plan 01):
      • dragStrength=0 ⇒ every tier terminal wealth equals its independent `analyticOrdinaryAnnuity` within relErr<1e-9 (D-12 baseline collapse, Pitfall 3).
      • Non-conservation: with dragStrength 0.3 then 0.6, aggregate real wealth at final year > aggregate at year 0 in BOTH cases (grows, not conserved); and pairwise tier deltas do not net to zero (no transfer — assert sum-of-tier-gains ≠ a conserved constant; Pitfall 4).
      • Infinite-growth preserved: under default synthetic params with drag on, no tier's real wealth series ever goes negative (Pitfall 3 warning sign).
      • Monotone divergence: higher dragStrength widens the top0.1−median terminal gap monotonically while no tier's nominal wealth shrinks (Pitfall 3 (c)).
    - basis.test.ts: extend Plan 01's unit-level assertion with an END-TO-END case — `projectionEngine(syntheticInputs, makeSyntheticParams({returnByTier tagged basis:'nominal'}))` throws `/Basis violation/` (MODEL-05, Pitfall 1).
    - numericalStability.test.ts: drag-off single tier, horizon 60, assert relErr<1e-9 at years {1,10,30,45,60} vs `analyticOrdinaryAnnuity` (Pitfall 10 / MODEL-06 / D-11) — engine must not drift.
    - multiTierFixture.test.ts: a hand-derived 2-3 tier, ~5-year, drag-ON fixture. Compute expected anchor wealths by hand (documented in test comments: the year-by-year top-set %, assetInflation, r_eff, ordinary-annuity step, and re-fit) and assert engine output matches within relErr<1e-6 (coupling pipeline incl. dynamic top set + re-fit; DIST_TOL because the curve re-fit uses the erf-precision distribution math, RESEARCH Open Question 2). NO hardcoded "18" anywhere; obviously-synthetic round inputs.
  </behavior>
  <implementation>
    RED: replace the remaining `it.todo` placeholders across the four test files with the assertions above; run — the new ones fail only if an engine bug exists (else they pass immediately, which is the goal: they are the acceptance gate). For multiTierFixture, FIRST hand-derive expected values (optionally via a `tsx` scratch computation using ONLY testUtils' independent annuity helper + closed-form Pareto, never the engine) and bake the literals into the test with a derivation comment.
    GREEN: if any invariant fails it indicates an engine defect from Plan 03 — fix the engine module at fault (tiers/drag/engine), not the test, then re-run the full suite.
    REFACTOR: ensure zero `it.todo`/`test.todo` remain in src/core/__tests__; run `npx vitest run --coverage` and confirm `src/core/drag.ts` and `src/core/distribution.ts` have coverage (RESEARCH phase gate).
  </implementation>
</feature>

<verify>
<automated>npx vitest run --coverage && npx tsc --noEmit && test "$(grep -rl 'it.todo\|test.todo' src/core/__tests__ | wc -l | tr -d ' ')" = "0"</automated>
</verify>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (in-engine) | NONE — pure offline numerical engine + its test suite; no I/O/network/untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-ZS | Tampering (model integrity) | engine drag coupling | mitigate | Non-conservation + no-transfer + infinite-growth tests assert P4/P3 invariants directly (MODEL-04) |
| T-04-BASIS | Tampering (data integrity) | engine boundary | mitigate | End-to-end basis.test.ts asserts nominal param throws (MODEL-05, P1) |
| T-04-FP | Denial / (correctness) | 60y compounding | mitigate | numericalStability.test.ts relErr<1e-9 at 5 sampled years vs independent closed form (MODEL-06, P10) |

No auth/session/crypto/network/web threats — framework-free in-process math + tests, null external surface (RESEARCH §"Security Domain").
</threat_model>

<verification>
- `npx vitest run --coverage` fully green; zero `it.todo` remaining anywhere in `src/core/__tests__`.
- All 6 MODEL requirement test rows from RESEARCH §"Phase Requirements → Test Map" have green automated commands.
- ROADMAP Phase 1 success criteria 3 (non-conservation/infinite-growth), 4 (basis), 5 (closed-form tolerance) demonstrably satisfied.
- `npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
The full D-12 invariant battery and the hand-derived multi-tier coupling fixture are green; the four highest-severity correctness pitfalls are provably mitigated; the engine + its test suite together constitute the trustworthy model the rest of the project builds on.
</success_criteria>

<output>
Create `.planning/phases/01-model-foundation/01-04-SUMMARY.md` when done.
</output>
