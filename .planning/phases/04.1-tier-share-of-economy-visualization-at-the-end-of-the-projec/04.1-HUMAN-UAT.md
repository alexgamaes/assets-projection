---
status: resolved
phase: 04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec
source: [04.1-VERIFICATION.md]
started: "2026-05-17T00:40:00Z"
updated: "2026-05-17T20:15:00Z"
---

## Current Test

[all tests resolved — G1 fixed and verified]

## Tests

### 1. Visual composition of Chart 4 and Chart 5
expected: Stacked-area bands fill the full chart height at every year (sum to 100%, no gap/overflow); concentration evolves over the horizon; donut shows 5 slices and evolves with horizon length.
result: PASSED (after G1 fix). Chart 4 now renders as a true 100%-stacked area: bands sum to exactly 1.0 every year, concentration rises (top-0.1% ~23%→~36% over the first decade) then plateaus, distinct per horizon (final top-0.1% 0.3596/0.3619/0.3635 for h35/h60/h100), 0 degraded years. Verified by direct band-data probe of the real engine (36/36 years valid) and by independent SSR re-render of selectShareOption/selectDonutOption across horizons 35/60/100. The misleading rank ("You") line was removed (rank lives in Chart 3). Fix commits: b6b10e7, 1271129, b75ea80 on branch fix/04.1-g1-chart4-band-shares.

### 2. Real/nominal toggle is basis-inert for Charts 4 and 5
expected: Toggling real/nominal does NOT change Chart 4 or Chart 5; each shows the D-11 basis-inert note.
result: PASSED. Charts 4/5 derive from rawResult (D-12 basis-invariant by construction — band shares are ratios independent of the real/nominal re-inflation applied only at display); D-11 note present in the chart components. Code-verified in AppShell composition (04.1-03) and unchanged by the G1 fix.

### 3. D-16 caption visible as DOM text
expected: Each chart shows the D-16 caption ending "...See the wealth-by-tier chart above for absolute amounts." as static visible text below the chart.
result: PASSED. SHARE_CAPTION rendered as a static <p> below both charts; byte-identical to NEUTRALITY-STYLE-GUIDE.md Section 7; asserted by selectors test (unchanged by the G1 fix).

### 4. Mobile responsive layout
expected: At narrow/mobile width sections stack vertically (stacked-area then donut); no horizontal scroll.
result: PASSED. Charts composed into AppShell's responsive column (04.1-03); ECharts containers are width-responsive; layout unchanged by the G1 fix.

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

### G1 — Chart 4 not 100%-stacked across the horizon (SC-2 failure)
status: resolved
test: 1
severity: blocking
summary: Two-layer defect. (1) selectShareOption supplied [x,y] pairs on a value x-axis, so ECharts silently ignored stack:'100%' and drew overlapping independent areas. (2) Under default endogenous evolution the distribution exceeded the finite-mean Pareto domain (top01/top1 ≥ 10, α ≤ 1) by ~year 11; calibrateCurve threw and the fallback froze band shares at the year-~10 shape, so charts/donut stopped evolving (also surfaced the misleading rank line and a legend/x-axis overlap).
resolution: |
  - Stacking: selectShareOption switched to a category x-axis with plain value arrays so ECharts honors the 100% stack; misleading rank "You" line removed; legend bottom-anchored with grid padding (commit 1271129).
  - Model (root cause): Closure A net-growth-floor added to the endogenous evolution (engine.ts) — top tiers' net growth floored at the body's, tail ratio asymptoted below RHO_MAX via a per-run tanh squash; concentration now rises then plateaus, α>1 every year through horizon 100, 0 degraded years. dragStrength re-derived (0.4326→0.2095) so the cited McKinsey ~80% asset-inflation target is still reproduced; CR-02 guard unchanged (commit b75ea80).
  - Defensive: honest "Beyond model domain (year N)" terminal state retained for residual extreme-input edges.
verification: Direct engine band-data probe (36/36 years valid, sum=1.0, degraded=false, distinct per horizon); independent SSR re-render of Chart 4 + donut across h35/h60/h100; calibration.test.ts reproduces ~80% at the re-derived dragStrength; full suite 167/167, typecheck clean, build ok.
likely_files: [src/state/selectors.ts, src/core/engine.ts, src/data/defaults.ts, src/core/tierBands.ts]
debug_sessions: [chart4-not-stacking, model-alpha-domain-freeze]
residual_caveat: Closure-A plateau lands at ~36% top-0.1% share vs the ~39% the comparison pass projected (same regime, softer asymptote); RHO_MAX/tanh shape are behavioral-contract modeling choices, not independently sourced (the cited McKinsey target drives dragStrength).
