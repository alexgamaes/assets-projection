---
status: diagnosed
phase: 04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec
source: [04.1-VERIFICATION.md]
started: "2026-05-17T00:40:00Z"
updated: "2026-05-17T00:52:00Z"
---

## Current Test

[Test 1 FAILED — Chart 4 does not stack to 100%]

## Tests

### 1. Visual composition of Chart 4 and Chart 5
expected: Stacked-area bands fill the full chart height at every year (sum to 100%, no gap/overflow); dashed teal "You" line drifts across bands. Donut shows 5 slices with a slate-800 outline on the user's slice; center text reads "Top X% hold ≥50% (year N)" with no exclamation mark.
result: FAILED — Chart 4 does not render as a 100%-stacked area. Instead the five bands draw as independent overlapping translucent triangles that do not sum to 100% at each year; the "Top 0.1%" band pins at 100% and stays flat while the others taper to zero by ~year 60–140. Stacking config in selectShareOption is correct (stack:'100%', areaStyle per band), so the defect is in deriveBandShares: per-year band shares are not normalized to sum to 1 across the horizon — consistent with code-review CR-01/WR-04 (calibrateCurve throws for drifted-distribution years, bare catch {} swallows it, fallback returns carried-forward/uniform values that break the stack). Screenshot evidence: Desktop/Screenshot 2026-05-16 at 5.50.19 PM.png.

### 2. Real/nominal toggle is basis-inert for Charts 4 and 5
expected: Toggling real/nominal does NOT change Chart 4 or Chart 5; each shows the D-11 note "Shares are identical in real and nominal terms — the real/nominal toggle does not affect this view."
result: [pending]

### 3. D-16 caption visible as DOM text
expected: Each chart shows the D-16 caption ending "...See the wealth-by-tier chart above for absolute amounts." as static visible text below the chart — not tooltip-only or collapsible.
result: [pending]

### 4. Mobile responsive layout
expected: At narrow/mobile width (e.g. 375×667) sections stack vertically (stacked-area then donut); no horizontal scroll.
result: [pending]

## Summary

total: 4
passed: 0
issues: 1
pending: 3
skipped: 0
blocked: 0

## Gaps

### G1 — Chart 4 not 100%-stacked across the horizon (SC-2 failure)
status: failed
test: 1
severity: blocking
summary: deriveBandShares produces per-year band shares that do not sum to 1 across the horizon, so the ECharts 100%-stack renders overlapping triangles instead of full-height stacked bands. Root-cause hypothesis: calibrateCurve throws for drifted-distribution years; the bare `catch {}` fallback in tierBands.ts returns carried-forward/uniform values that break stack normalization (see 04.1-REVIEW.md CR-01, WR-04, WR-05).
likely_files: [src/core/tierBands.ts, src/state/selectors.ts]
suggested_fix: Investigate calibrateCurve failure frequency across horizon years; replace the degenerate fallback with a per-year renormalization (or surface a degraded flag and suppress the band rather than emit non-summing values). Re-verify that band shares sum to 1±tol for every horizon year.

