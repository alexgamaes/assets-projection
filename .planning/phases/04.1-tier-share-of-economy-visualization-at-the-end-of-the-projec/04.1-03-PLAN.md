---
phase: 04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec
plan: 03
type: execute
wave: 2
depends_on: ["04.1-01", "04.1-02"]
files_modified:
  - docs/NEUTRALITY-STYLE-GUIDE.md
  - src/ui/AppShell.tsx
autonomous: false
requirements: [VIZ-07]
must_haves:
  truths:
    - "AppShell renders the stacked-area chart (Chart 4) and the donut (Chart 5) after Chart 3 and before CitationFooter, inside the existing rawResult!==null branch"
    - "Both new charts derive band shares from rawResult (NOT the reinflated result) — basis-invariant (D-12)"
    - "Each chart shows the verbatim D-16 caption (via caption prop) and a D-11 basis-inert note"
    - "NEUTRALITY-STYLE-GUIDE.md Section 7 (D-16) exists, byte-identical caption template to SHARE_CAPTION"
    - "If bandShares is null (calibration-fallback failure) the section degrades to the existing neutral diagnostic (no stack trace)"
  artifacts:
    - path: "docs/NEUTRALITY-STYLE-GUIDE.md"
      provides: "Section 7 — D-16 Share-of-Economy Neutral Disclosure Rule"
      contains: "D-16"
    - path: "src/ui/AppShell.tsx"
      provides: "bandShares/shareOption/donutOption useMemo chain + two new <section> blocks + D-11 note"
      contains: "deriveBandShares"
  key_links:
    - from: "src/ui/AppShell.tsx"
      to: "src/core/tierBands.ts"
      via: "deriveBandShares(rawResult.series) in useMemo"
      pattern: "deriveBandShares\\(rawResult"
    - from: "src/ui/AppShell.tsx"
      to: "src/state/selectors.ts"
      via: "selectShareOption / selectDonutOption / SHARE_CAPTION import"
      pattern: "selectShareOption"
---

<objective>
Compose VIZ-07 into the running app: wire `deriveBandShares` →
`selectShareOption`/`selectDonutOption` into the existing AppShell
`useDeferredValue → try/catch useMemo → null-guard → selectors` chain, render
the two new `<section>` blocks (stacked-area Chart 4, donut Chart 5) with the
D-16 caption and D-11 basis-inert note, and author the D-16 rule into
`NEUTRALITY-STYLE-GUIDE.md` Section 7 (byte-identical to `SHARE_CAPTION`,
Phase 5 NEUT-02 gate).

This is the final vertical-slice closure: after this plan a user opening the
app sees both new charts composed below the existing three.

Purpose: ROADMAP Phase 4.1 criteria 2, 3, 5 — charts composed into AppShell,
mandatory neutral caption, Phase-5-gated style-guide rule.
Output: extended `AppShell.tsx`, new Section 7 in `NEUTRALITY-STYLE-GUIDE.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec/04.1-CONTEXT.md
@.planning/phases/04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec/04.1-RESEARCH.md
@.planning/phases/04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec/04.1-PATTERNS.md
@.planning/phases/04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec/04.1-UI-SPEC.md
@.planning/phases/04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec/04.1-02-SUMMARY.md

<interfaces>
From src/state/selectors.ts (Plan 02):
- selectShareOption(bandSeries: BandShare[], result): EChartsOption
- selectDonutOption(bandSeries: BandShare[], result): EChartsOption
- export const SHARE_CAPTION: string (verbatim D-16 template)

From src/core/tierBands.ts (Plan 01):
- deriveBandShares(series: YearSnapshot[]): BandShare[]

From src/viz (Plan 02):
- TierShareChart({ option, caption, style? })
- DonutChart({ option, caption, style? })

Existing AppShell.tsx pattern to MIRROR (lines 26-31 caption constants
REL_POS_CAPTION/ENGINE_ERROR_MSG; lines 42-73 memo chain; lines 70-73
relOption uses rawResult — D-08 basis-independent precedent; lines 99-141
JSX inside `rawResult === null ? <ENGINE_ERROR_MSG> : <>...charts...</>`
guard; section wrapper class `bg-slate-50 border border-slate-200 p-4
rounded`; `.js` ESM imports).

LOCKED placement (UI-SPEC): append AFTER Chart 3 (relative-position),
BEFORE <CitationFooter>, inside the rawResult!==null branch in
`<main className="space-y-6">`. Order: Chart3 → Stacked-area(Chart4) →
Donut(Chart5) → CitationFooter. Full-width single-column; no new breakpoints.

LOCKED copy (UI-SPEC Copywriting Contract):
- Stacked-area <h2>: "Share of total wealth by tier over time"
- Donut <h2>: "Final-year wealth share by tier"
- D-11 basis-inert note (both, AppShell-rendered, NOT chart-rendered):
  "Shares are identical in real and nominal terms — the real/nominal toggle does not affect this view."
  as <p className="text-sm font-normal text-slate-500 mt-1">
- D-16 caption (both, rendered BY the chart component via caption prop, the
  byte-identical SHARE_CAPTION value) as <p className="text-base font-normal
  text-slate-600 mt-2"> (text-base 16px — parity with D-15).
- DOM order per section: <h2> → chart canvas → D-11 note(mt-1) → D-16 caption(mt-2)

VERBATIM D-16 template (Section 7 + SHARE_CAPTION — MUST be byte-identical):
"This shows each tier's share of total projected wealth. Shares can shift over time while every tier's real wealth still grows — a falling share does not mean falling wealth, only that another tier is compounding faster. See the wealth-by-tier chart above for absolute amounts."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author D-16 rule into NEUTRALITY-STYLE-GUIDE.md Section 7</name>
  <files>docs/NEUTRALITY-STYLE-GUIDE.md</files>
  <read_first>
    - docs/NEUTRALITY-STYLE-GUIDE.md Section 6 lines 141-168 (D-15 Rank-Delta rule — exact structural mirror: Seeded date, Phase 5 gate, Context, Required Caption Template, Constraints, Neutrality Test) and Section 3 line 86 (sync-constraint sentence to mirror)
    - .planning/phases/04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec/04.1-RESEARCH.md Pattern 6 (lines 457-477) and 04.1-PATTERNS.md NEUTRALITY-STYLE-GUIDE section (lines 200-224)
    - src/state/selectors.ts (confirm SHARE_CAPTION value to keep byte-identical)
  </read_first>
  <action>
    Append `## Section 7 — D-16 Share-of-Economy Neutral Disclosure Rule` to
    `docs/NEUTRALITY-STYLE-GUIDE.md`, mirroring Section 6's structure exactly:
    - `**Seeded:** 2026-05-16 (Phase 4.1, Plan 03)`
    - `**Phase 5 gate:** NEUT-02 reviewer will verify the rendered caption
      matches this template verbatim.`
    - `### Context` — shares can shift over time while every band's real
      wealth still grows; a falling share is not falling wealth (no zero-sum /
      no finite-pie), consistent with the model's non-conservation property.
    - `### Required Caption Template (D-16)` — a blockquote containing the
      VERBATIM D-16 string from the interfaces block, byte-identical to
      `SHARE_CAPTION` in `src/state/selectors.ts`.
    - `### Constraints` — visible DOM `<p>` element, not collapsible,
      not tooltip-only, on/near BOTH charts; include the sync-constraint
      sentence mirroring Section 3 line 86: any revision to this text
      requires a corresponding update to `SHARE_CAPTION` (the two must
      remain in sync).
    - `### Neutrality Test` — mechanistic framing, no alarm punctuation, no
      editorializing (mirror Section 6's closing wording).
    Do not alter Sections 1-6.
  </action>
  <verify>
    <automated>grep -q "Section 7 — D-16" docs/NEUTRALITY-STYLE-GUIDE.md && grep -q "a falling share does not mean falling wealth" docs/NEUTRALITY-STYLE-GUIDE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Section 7 — D-16" docs/NEUTRALITY-STYLE-GUIDE.md` present.
    - The blockquote caption text in Section 7 is byte-identical to SHARE_CAPTION (manually diff the two strings; both contain "See the wealth-by-tier chart above for absolute amounts.").
    - Section 7 contains "Seeded:", "Phase 5 gate:", "### Context", "### Required Caption Template", "### Constraints", "### Neutrality Test" sub-structure (Section 6 mirror).
    - Sections 1-6 unchanged (`git diff` touches only appended content).
  </acceptance_criteria>
  <done>Section 7 / D-16 authored mirroring Section 6; caption blockquote byte-identical to SHARE_CAPTION; Sections 1-6 untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Compose Chart 4 + Chart 5 into AppShell</name>
  <files>src/ui/AppShell.tsx</files>
  <read_first>
    - src/ui/AppShell.tsx (entire file — caption constants region ~26-31, memo chain 42-73 esp. relOption/rawResult D-08 precedent 70-73, JSX guard + section wrappers 99-141)
    - src/viz/RelativePosChart.tsx (caption-as-prop contract the new charts follow)
    - src/state/selectors.ts (selectShareOption, selectDonutOption, SHARE_CAPTION signatures)
    - src/core/tierBands.ts (deriveBandShares signature)
    - .planning/phases/04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec/04.1-RESEARCH.md Pattern 5 (lines 377-453) and 04.1-UI-SPEC.md Caption/Responsive placement sections
  </read_first>
  <action>
    Extend `src/ui/AppShell.tsx`. Add `.js` ESM imports:
    `deriveBandShares` from `'../core/tierBands.js'`; `selectShareOption,
    selectDonutOption, SHARE_CAPTION` from `'../state/selectors.js'`;
    `TierShareChart` from `'../viz/TierShareChart.js'`; `DonutChart` from
    `'../viz/DonutChart.js'`.

    Add three `useMemo`s mirroring the existing `relOption` (D-08
    rawResult-based) precedent — band shares are basis-invariant (D-12) so
    they MUST derive from `rawResult`, NOT the reinflated `result`:
    - `bandShares = useMemo(() => rawResult !== null ?
      deriveBandShares(rawResult.series) : null, [rawResult])`
    - `shareOption = useMemo(() => (bandShares !== null && rawResult !== null)
      ? selectShareOption(bandShares, rawResult) : null,
      [bandShares, rawResult])`
    - `donutOption = useMemo(() => (bandShares !== null && rawResult !== null)
      ? selectDonutOption(bandShares, rawResult) : null,
      [bandShares, rawResult])`
    Add a one-line comment mirroring the existing D-08 `// Chart 3 uses
    rawResult` comment style, noting D-12 basis-invariance.

    In the JSX, inside the existing `rawResult === null ? <ENGINE_ERROR_MSG>
    : <>...</>` guard, AFTER the Chart 3 (relative-position) `<section>` and
    BEFORE `<CitationFooter>`, add two `<section className="bg-slate-50
    border border-slate-200 p-4 rounded">` blocks (reuse the EXACT existing
    wrapper class):
    - Chart 4: `<h2 className="text-[20px] font-semibold text-slate-800
      mb-4">Share of total wealth by tier over time</h2>`, then guard
      `shareOption !== null` — render `<TierShareChart option={shareOption}
      caption={SHARE_CAPTION} />`; if `shareOption === null`, render nothing
      for the chart (the section degrades; no stack trace — Pitfall 2 /
      UI-SPEC empty state). After the chart, the D-11 note `<p
      className="text-sm font-normal text-slate-500 mt-1">Shares are
      identical in real and nominal terms — the real/nominal toggle does not
      affect this view.</p>` (AppShell-rendered; the D-16 caption is rendered
      INSIDE TierShareChart via its caption prop, DOM order: chart → D-11
      note → D-16 caption-from-component).
    - Chart 5: `<h2 ...>Final-year wealth share by tier</h2>`, same
      donutOption null-guard, `<DonutChart option={donutOption}
      caption={SHARE_CAPTION} />`, same D-11 note.
    Do not change the existing three chart sections, the control panel, or
    the responsive grid wrapper.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "deriveBandShares(rawResult" src/ui/AppShell.tsx` present (D-12 — derives from rawResult, NOT result).
    - `grep -n "selectShareOption\|selectDonutOption" src/ui/AppShell.tsx` both present.
    - `grep -n "Share of total wealth by tier over time\|Final-year wealth share by tier" src/ui/AppShell.tsx` shows both <h2> strings.
    - `grep -n "Shares are identical in real and nominal terms" src/ui/AppShell.tsx` present (D-11 note).
    - `grep -n "caption={SHARE_CAPTION}" src/ui/AppShell.tsx` shows it passed to BOTH chart components.
    - `npm test` full suite green AND `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>AppShell renders Chart 4 + Chart 5 after Chart 3 / before CitationFooter, band shares from rawResult (D-12), D-16 caption via prop, D-11 note AppShell-rendered, null-guard degradation; full suite green + tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    VIZ-07 composed into the running app: a 100%-stacked-area chart (5
    population bands poorest-at-bottom + dashed teal "You" position line) and
    an end-of-horizon donut (5 bands, slate-800 outline on the user's band,
    top-set concentration center label), both after the existing three charts,
    each with the D-11 basis-inert note and the D-16 neutral caption.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app URL.
    2. Confirm BELOW the existing three charts: a stacked-area chart titled
       "Share of total wealth by tier over time" then a donut titled
       "Final-year wealth share by tier".
    3. Stacked-area: bands always fill the full chart height at every year
       (sum to 100% — no white gap/overflow at the top); a dashed teal line
       labelled "You" drifts across bands over the horizon.
    4. Donut: 5 slices, the slice containing your percentile at the final
       year has a dark (slate-800) outline; center text reads
       "Top X% hold ≥50% (year N)" with no exclamation mark.
    5. Toggle the real/nominal switch: BOTH new charts do NOT change; each
       shows the note "Shares are identical in real and nominal terms — the
       real/nominal toggle does not affect this view."
    6. Each chart shows the D-16 caption ending "...See the wealth-by-tier
       chart above for absolute amounts." as visible text (not a tooltip).
    7. Resize to a narrow/mobile width: sections stack (stacked-area then
       donut), no horizontal scroll.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rawResult (engine output) -> AppShell render | Numeric model output only; engine errors contained by existing try/catch null-guard |
| Style-guide template <-> SHARE_CAPTION constant | Must stay byte-identical; Phase 5 NEUT-02 verifies rendered DOM == template |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-05 | Denial of Service | bandShares null / calibration-fallback failure crashing render | mitigate | bandShares/shareOption/donutOption null-guards mirror existing rawResult guard; section degrades to existing neutral diagnostic, no stack trace (UI-SPEC empty state) |
| T-04.1-06 | Tampering / Repudiation | D-16 caption text drifting from style-guide Section 7 | mitigate | Task 1 authors Section 7 byte-identical to SHARE_CAPTION; sync-constraint sentence documented; Plan 02 test locks SHARE_CAPTION; Phase 5 NEUT-02 re-verifies rendered DOM |
| T-04.1-03 | Information disclosure (XSS in rendered labels) | AppShell <h2>/<p> + chart captions | mitigate | All strings are static literals or numeric-derived model output (T-03-03); no user-entered string interpolated into rendered text |
| T-04.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npm test` full suite green + `npx tsc --noEmit` clean
- `deriveBandShares(rawResult...)` in AppShell (NOT reinflated result — D-12)
- NEUTRALITY-STYLE-GUIDE Section 7 caption byte-identical to SHARE_CAPTION
- Human-verify checkpoint approved (visual composition, basis-inert behavior, mobile stacking, no stack trace on degenerate curve)
- Phase 5 NEUT-02 checklist note: add SHARE_CAPTION + the two D-11 notes to the caption-equality verification list
</verification>

<success_criteria>
- ROADMAP Phase 4.1 all six success criteria satisfied: (1) per-tier share series [Plan 01], (2) stacked-area composed into AppShell, (3) end-of-horizon donut, (4) existing palette no new hue [Plan 02], (5) mandatory neutral D-16 caption + style-guide rule, (6) basis-invariant view asserted in tests [Plan 01] and inert in UI (D-11).
- VIZ-07 fully delivered; Phase 5 can review this chart's copy/palette.
</success_criteria>

<output>
Create `.planning/phases/04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec/04.1-03-SUMMARY.md` when done
</output>
