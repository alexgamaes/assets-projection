---
phase: 04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/core/tierBands.ts
  - src/core/__tests__/tierBands.test.ts
autonomous: true
requirements: [VIZ-07]
must_haves:
  truths:
    - "deriveBandShares returns 5 population bands per horizon year that sum to exactly 1.0 (within DIST_TOL)"
    - "Band shares match the closed-form Pareto(alpha=2) fixture for the five population boundaries"
    - "Band shares are byte-identical under real vs nominal basis (D-12 basis-invariance)"
    - "Calibration failure falls back gracefully (no thrown error propagates to callers)"
  artifacts:
    - path: "src/core/tierBands.ts"
      provides: "deriveBandShares pure function + BandShare type"
      exports: ["deriveBandShares", "BandShare"]
      contains: "cumulativeShareFromTop"
    - path: "src/core/__tests__/tierBands.test.ts"
      provides: "VIZ-07 engine tests: band-sum, Pareto fixture, D-12 basis-invariance, fallback"
  key_links:
    - from: "src/core/tierBands.ts"
      to: "src/core/distribution.ts"
      via: "calibrateCurve + cumulativeShareFromTop import"
      pattern: "cumulativeShareFromTop\\(curve"
---

<objective>
Phase 4.1 delivers VIZ-07: a tier share-of-economy view. This plan delivers the
pure engine foundation — `deriveBandShares`, the framework-free function that
turns the projection's per-year `YearSnapshot[]` into per-year five-band
Lorenz-mass fractions — and its full Wave-0 test suite covering band-sum,
the Pareto(alpha=2) closed-form fixture, and D-12 basis-invariance.

This is the Wave-0 foundation for the whole phase. Plans 02 and 03 (selectors,
chart components, AppShell composition) all consume the `BandShare` type and
`deriveBandShares` produced here.

Purpose: Per-band wealth-mass + share-of-total is the data primitive every other
artifact in this phase renders. It must be pure, tested, and basis-invariant
before any UI exists (correctness-first, CLAUDE.md).
Output: `src/core/tierBands.ts`, `src/core/__tests__/tierBands.test.ts`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec/04.1-CONTEXT.md
@.planning/phases/04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec/04.1-RESEARCH.md
@.planning/phases/04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec/04.1-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Boundary decision: build Anchors LOCALLY
     in tierBands.ts (PATTERNS.md option a). anchorWealthToAnchors and
     shiftScaleCurve are PRIVATE in engine.ts — DO NOT export them, DO NOT
     import from engine.ts. tierBands.ts stays decoupled from engine.ts. -->

From src/core/distribution.ts:
- export interface Curve { lognormal:{mu,sigma}; pareto:{alpha,xm}; stitchQuantile; totalWealth }
- export function calibrateCurve(anchors: Anchors): Curve
    // Anchors = { median: SourcedParam; top10: SourcedParam; top1: SourcedParam; top01: SourcedParam }
    // reads anchors.<k>.value only
- export function cumulativeShareFromTop(curve: Curve, p: number): number
    // returns wealth-share held by the top (1-p) of population, in [0,1]
    // p<=0 -> 1 ; p>=1 -> 0 ; closed-form lognormal+Pareto

From src/core/types.ts:
- YearSnapshot carries: anchorWealth ({median,top10,top1,top01}), userWealth,
  userPercentile, topSetPercentile, and an internal _totalWealth field.

From src/core/relativePosition.ts (STRUCTURAL ANALOG — mirror its shape):
- export function deriveShares(series: YearSnapshot[]): Array<{year;userShare;userRank}>
    // pure series.map(snap => ...) — deriveBandShares follows the same shape

From src/core/__tests__/testUtils.ts:
- DIST_TOL = 1e-6
- synParam(value, basis?, note?) -> SourcedParam   // build synthetic Anchors
- makeSyntheticParams(overrides?), syntheticInputs

Engine fallback idiom to MIRROR (engine.ts:274-284) — calibrateCurve can throw
the CR-02 alpha<=1 guard for evolved distributions; engine catches and falls
back to shiftScaleCurve. Since shiftScaleCurve is private and unreachable here,
deriveBandShares MUST catch the calibration error and fall back to the PRIOR
year's BandShare (or, for year 0, a degenerate-but-summing-to-1 entry). A
caught error must NEVER propagate out of deriveBandShares.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deriveBandShares pure function</name>
  <files>src/core/tierBands.ts</files>
  <read_first>
    - src/core/relativePosition.ts (deriveShares shape to mirror — series.map, _totalWealth read pattern, .js import convention)
    - src/core/distribution.ts lines 56-66 (Curve interface), 286-374 (calibrateCurve + Anchors shape), 468-517 (cumulativeShareFromTop)
    - src/core/engine.ts lines 162-177 (anchorWealthToAnchors — replicate this Anchors-construction logic LOCALLY; do NOT import it), 269-284 (try/catch calibrate fallback idiom)
    - src/core/types.ts (YearSnapshot, _totalWealth internal field, SourcedParam, AnchorWealth)
    - src/core/__tests__/testUtils.ts (synParam signature for the test in Task 2; informs the local SourcedParam wrapper shape)
  </read_first>
  <behavior>
    - deriveBandShares(series) returns a BandShare[] of the same length as series, one entry per year.
    - Each BandShare has: year, bottom50, band50to90, band90to99, band99to999, top01 — all fractions in [0,1].
    - Per-year math: s50=cumulativeShareFromTop(curve,0.5), s90=...(0.9), s99=...(0.99), s999=...(0.999); bottom50=1-s50, band50to90=s50-s90, band90to99=s90-s99, band99to999=s99-s999, top01=s999.
    - The five values are normalized so they sum to exactly 1.0 (divide each by their sum; guard sum<=0 -> use factor 1).
    - For each year the curve is reconstructed via calibrateCurve on a locally-built Anchors object from snap.anchorWealth.
    - On calibrateCurve throw for a year: that year reuses the previous year's BandShare values (year field still that year's year); if it is the first year, return a uniform degenerate entry that still sums to 1.0.
    - No thrown error escapes deriveBandShares.
  </behavior>
  <action>
    Create `src/core/tierBands.ts` as a framework-free pure module mirroring
    `relativePosition.ts` (`series.map(snap => ...)`, `.js` ESM relative
    imports — NodeNext requires `.js` even for `.ts` sources).

    Import `{ calibrateCurve, cumulativeShareFromTop, type Curve }` from
    `'./distribution.js'` and `type { YearSnapshot }` from `'./types.js'`.
    Do NOT import anything from `'./engine.js'` (anchorWealthToAnchors and
    shiftScaleCurve are private — boundary decision: build Anchors locally).

    Export `interface BandShare { year: number; bottom50: number;
    band50to90: number; band90to99: number; band99to999: number;
    top01: number }` with the population-band semantics documented in
    RESEARCH Pattern 1 (Bottom 50% = population [0,0.5), ... Top 0.1% =
    [0.999,1]).

    Local Anchors construction: write a private helper that takes
    `snap.anchorWealth` ({median,top10,top1,top01} numbers) and returns an
    object shaped `{ median:{value}, top10:{value}, top1:{value},
    top01:{value} }` sufficient for `calibrateCurve` (which only reads
    `.value`). Mirror engine.ts:166-177 logic but build the wrapper objects
    locally — do not depend on params; a minimal `{ value }` SourcedParam-like
    shape is acceptable since calibrateCurve only reads `.value`. If
    calibrateCurve's `Anchors` type requires more fields, replicate the
    minimal SourcedParam shape from `synParam` (value + basis + note) inline.

    Per-year compute: build curve via try/calibrateCurve; on catch, fall back
    to the previous accumulated BandShare (carry-forward) per the engine
    fallback idiom — since shiftScaleCurve is unreachable. For year index 0
    failure, emit a degenerate entry with all five bands = 0.2 (sums to 1.0,
    AppShell null-guard / downstream handles visual). Compute s50/s90/s99/s999
    via `cumulativeShareFromTop(curve, 0.5|0.9|0.99|0.999)`, derive the five
    raw band values, then normalize: `total = sum`; `f = total > 0 ? 1/total
    : 1`; multiply each band by `f` (Pitfall 3 float-sum correction).

    Export `function deriveBandShares(series: YearSnapshot[]): BandShare[]`.
    Read `_totalWealth` only if needed for diagnostics — it is NOT required
    for band math (cumulativeShareFromTop already returns fractions of the
    curve's total).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/tierBands.ts` exists; exports `deriveBandShares` and `BandShare`.
    - `grep -n "from './engine" src/core/tierBands.ts` returns NOTHING (no engine.ts coupling).
    - `grep -n "cumulativeShareFromTop(curve, 0.5\|0.9\|0.99\|0.999" src/core/tierBands.ts` shows the four boundary calls.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>tierBands.ts compiles clean; deriveBandShares + BandShare exported; no engine.ts import; normalization-to-1.0 and calibration carry-forward fallback present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave-0 test suite — band-sum, Pareto fixture, D-12 basis-invariance</name>
  <files>src/core/__tests__/tierBands.test.ts</files>
  <read_first>
    - src/core/__tests__/distribution.test.ts lines 26-60 (Pareto(alpha=2) known fixture: cumulativeShareFromTop(curve,p)=sqrt(1-p); imports + describe/it/expect pattern)
    - src/core/__tests__/testUtils.ts (DIST_TOL=1e-6, synParam, makeSyntheticParams, syntheticInputs)
    - src/core/tierBands.ts (the module under test, from Task 1)
    - src/core/relativePosition.ts (to build a real YearSnapshot[] via projectionEngine for the basis-invariance test) and src/core/engine.ts:193 (projectionEngine signature)
    - src/state/selectors.ts (selectReinflated — used to produce the nominal-basis series for the D-12 byte-identical assertion)
  </read_first>
  <behavior>
    - Test A (band-sum): for a projected series, every BandShare's five fields sum to within DIST_TOL of 1.0.
    - Test B (Pareto fixture): for a Pareto(alpha=2, xm=1) curve, expected bands are bottom50≈1-sqrt(0.5)≈0.2929, band50to90≈sqrt(0.5)-sqrt(0.1)≈0.3909, band90to99≈sqrt(0.1)-sqrt(0.01)≈0.2162, band99to999≈sqrt(0.01)-sqrt(0.001)≈0.0684, top01≈sqrt(0.001)≈0.03162; each asserted within DIST_TOL.
    - Test C (D-12 basis-invariance): deriveBandShares on the raw (real) series and on the nominal-basis-derived series produce arrays that are deep-equal (expect(real).toEqual(nominal)).
    - Test D (fallback): a series whose anchors force calibrateCurve to throw does not raise; deriveBandShares returns a same-length array and no entry contains NaN.
  </behavior>
  <action>
    Create `src/core/__tests__/tierBands.test.ts` mirroring
    `distribution.test.ts` imports (`describe, it, expect` from `'vitest'`;
    `DIST_TOL, synParam` from `'./testUtils.js'`).

    Test A & C: build a real `YearSnapshot[]` by calling
    `projectionEngine(syntheticInputs, makeSyntheticParams())` (import
    projectionEngine from `'../engine.js'`). Assert each year's five bands
    sum within `DIST_TOL` of 1.0. For C, produce the nominal series the same
    way the app does (use `selectReinflated` from `'../../state/selectors.js'`
    with the 'nominal' basis if its signature supports it; otherwise
    reinflate anchorWealth by (1+i)^year inline) and assert
    `expect(deriveBandShares(rawSeries)).toEqual(deriveBandShares(nominalSeries))`.

    Test B: construct the Pareto(alpha=2, xm=1) curve using the SAME
    construction `distribution.test.ts` uses for its known fixture (reuse its
    synParam-based Anchors or direct Curve literal). Since deriveBandShares
    takes a series, build a one-element synthetic snapshot whose reconstructed
    curve is the Pareto(alpha=2) curve — OR add an internal seam by testing
    the per-year band math against the known closed-form values
    (sqrt(1-p)). Assert each band within `DIST_TOL` of the values above and
    the sum within `DIST_TOL` of 1.0.

    Test D: feed anchors that trigger the CR-02 alpha<=1 guard (top01/top1
    ratio extreme — mirror the evolved-distribution case from engine
    fallback). Assert `expect(() => deriveBandShares(series)).not.toThrow()`,
    output length equals input length, and no field is NaN
    (`Number.isFinite` on every band of every entry).
  </action>
  <verify>
    <automated>npm test -- tierBands</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tierBands` runs and ALL tests pass (exit 0).
    - Test output shows at least 4 passing test cases (band-sum, Pareto fixture, basis-invariance, fallback-no-throw).
    - `grep -n "toEqual" src/core/__tests__/tierBands.test.ts` shows the D-12 deep-equality assertion.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>tierBands.test.ts green; band-sum, Pareto(alpha=2) closed-form, D-12 basis-invariance (toEqual), and fallback-no-throw all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zustand params -> engine -> deriveBandShares | Numeric model parameters only; no user-supplied strings reach this code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Tampering | deriveBandShares numeric output (NaN/Infinity from calibration edge cases) | mitigate | try/catch calibration fallback + Test D asserts no NaN and no throw; normalization guards sum<=0 |
| T-04.1-02 | Denial of Service | per-year calibrateCurve recompute over long horizon | accept | calibrateCurve is sub-millisecond closed-form (RESEARCH); ~60 year horizon is negligible; no untrusted input controls horizon length beyond existing capped slider |
| T-04.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: not applicable; all deps already installed and in production use) |
</threat_model>

<verification>
- `npm test -- tierBands` green (band-sum within DIST_TOL, Pareto(alpha=2) fixture, D-12 basis-invariance toEqual, fallback no-throw/no-NaN)
- `npx tsc --noEmit` clean
- `src/core/tierBands.ts` has zero imports from `./engine` (boundary decision honored)
</verification>

<success_criteria>
- ROADMAP Phase 4.1 success criterion 1 satisfied at the engine layer: per-tier wealth mass + fraction-of-total computed per horizon year.
- ROADMAP success criterion 6 (basis-invariance) asserted in tests (D-12).
- VIZ-07 engine foundation complete and pure (no UI/framework imports).
</success_criteria>

<output>
Create `.planning/phases/04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec/04.1-01-SUMMARY.md` when done
</output>
