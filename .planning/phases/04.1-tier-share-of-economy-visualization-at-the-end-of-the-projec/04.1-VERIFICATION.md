---
phase: 04.1-tier-share-of-economy-visualization-at-the-end-of-the-projec
verified: 2026-05-17T20:15:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
human_verification_status: resolved
human_verification_resolution: |
  All 4 human-verification items resolved during the G1 debug cycle. The blocking
  item (Chart 4 stacking / evolution) initially FAILED on real-app inspection,
  triggering debug sessions chart4-not-stacking and model-alpha-domain-freeze.
  Both root causes (ECharts [x,y]-on-value-axis stacking landmine; endogenous
  evolution exceeding the finite-mean Pareto domain) were fixed (commits
  b6b10e7, 1271129, b75ea80) and independently verified: direct engine
  band-data probe (36/36 years valid, sum=1.0, no degraded, distinct per
  horizon), independent SSR re-render of Charts 4/5 across horizons 35/60/100,
  calibration.test.ts reproducing the cited McKinsey ~80% at the re-derived
  dragStrength, full suite 167/167, typecheck clean, build ok. See
  04.1-HUMAN-UAT.md (status: resolved, 4/4 passed) and
  .planning/debug/resolved/ for the full trace.
human_verification:
  - test: "Visual composition of Chart 4 (stacked-area) and Chart 5 (donut)"
    expected: |
      Charts 4 and 5 render below the existing three charts. Stacked-area bands fill the
      full chart height at every year (no white gap at the top). A dashed teal line
      labelled 'You' drifts across bands over the horizon. Donut shows 5 slices; the
      slice containing the user's final-year percentile has a dark (slate-800) outline.
      Center text reads 'Top X% hold ≥50% (year N)' with no exclamation mark.
    why_human: "ECharts canvas rendering and stacking correctness cannot be verified by grep or static analysis."
  - test: "Real/nominal toggle leaves Charts 4 and 5 unchanged"
    expected: |
      Toggling the real/nominal switch does not change Chart 4 or Chart 5.
      Both sections show the note 'Shares are identical in real and nominal terms —
      the real/nominal toggle does not affect this view.'
    why_human: "Requires running the app and observing live DOM behavior on state change."
  - test: "D-16 caption is visible as DOM text (not tooltip-only)"
    expected: |
      Below each of Chart 4 and Chart 5, the caption text ending
      '...See the wealth-by-tier chart above for absolute amounts.' is visible as
      static text in the page, not inside a tooltip or hidden behind an interaction.
    why_human: "Requires visual inspection of the rendered page to confirm DOM visibility vs tooltip-only."
  - test: "Mobile responsive: sections stack without horizontal scroll"
    expected: |
      At a narrow/mobile viewport, the stacked-area section and donut section stack
      vertically with no horizontal overflow or scroll.
    why_human: "Requires browser resize or device emulation to verify."
---

# Phase 4.1: VIZ-07 Tier Share-of-Economy Visualization Verification Report

**Phase Goal:** Make wealth concentration legible as a share of the whole: each percentile tier (median p50, top10 p90, top1 p99, top01 p99.9) and the user are shown as a fraction of total distribution wealth, both as a trajectory over the horizon and as a final-year snapshot — so the reader sees not just that tiers diverge in level but how the composition of total wealth shifts over time.

**Verified:** 2026-05-17T17:40:00Z
**Status:** human_needed — all 5 automated must-haves VERIFIED; 4 visual behaviors require human confirmation
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Engine/selector layer computes each tier's wealth mass and its fraction of total distribution wealth per horizon year | VERIFIED | `deriveBandShares` in `src/core/tierBands.ts` maps `YearSnapshot[]` → `BandShare[]` with five Lorenz-mass fractions (D-01) summing to 1.0 per year. 12/12 tests pass including D-01 band-sum, D-02 five fixed boundaries, and D-12 basis-invariance. |
| 2 | A 100%-stacked-area chart renders each tier's + the user's share of total wealth across every horizon year, composed into AppShell | VERIFIED | `selectShareOption` produces 6 series (5 stacked bands + 1 user line on hidden yAxisIndex:1). `TierShareChart` wrapper wired in `AppShell.tsx` inside the `rawResult !== null` branch after Chart 3, before CitationFooter. `bandShares` derived from `rawResult` (D-12). |
| 3 | An end-of-horizon donut summarizes the final-year share split for the same groups | VERIFIED | `selectDonutOption` produces a `pie` series with `radius:['40%','70%']`, 5 data items in D-09 order, D-07 `borderColor:'#1E293B'` on the user's band, and D-08 graphic center label. `DonutChart` wired in AppShell after Chart 4. |
| 4 | Both charts reuse the existing categorical palette — no semantic red/green, no new hue (D-03) | VERIFIED | Band hues mapped exclusively to `COLORS.median`, `COLORS.top10`, `COLORS.top1`, `COLORS.top01`, `COLORS.user` — all constants present in the pre-existing `COLORS` object. No new color literal introduced. `selected:true` absent (Pitfall 4 avoided). |
| 5 | Each chart carries a mandatory neutral caption clarifying shares can shift while every tier's real wealth still grows (no zero-sum implication), consistent with the D-15 disclosure pattern | VERIFIED | `SHARE_CAPTION` exported from `selectors.ts` (line 418) contains the verbatim D-16 string byte-identical to `NEUTRALITY-STYLE-GUIDE.md` Section 7 blockquote. `caption={SHARE_CAPTION}` passed to both `TierShareChart` and `DonutChart` in AppShell (lines 162, 175). Both wrappers render the caption as `<p className="text-base font-normal text-slate-600 mt-2">`. Section 7 authored in `docs/NEUTRALITY-STYLE-GUIDE.md`. Selector test asserts byte-exact equality. |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/core/tierBands.ts` | `deriveBandShares` + `BandShare` type | VERIFIED | File exists, exports `deriveBandShares` and `BandShare`. No `./engine` import (boundary decision honored). Imports from `./distribution.js` only. |
| `src/core/__tests__/tierBands.test.ts` | Wave-0 test suite: band-sum, Pareto fixture, D-12 basis-invariance, fallback | VERIFIED | 12 tests across 4 describe blocks. All 12 pass. Covers D-01, D-02, D-12, T-04.1-01. |
| `src/state/selectors.ts` | `selectShareOption`, `selectDonutOption`, `SHARE_CAPTION`, `BandShare` re-export | VERIFIED | All four exports present (lines 405, 418, 440, 565). |
| `src/viz/TierShareChart.tsx` | Thin `notMerge` wrapper with caption prop | VERIFIED | File exists, `notMerge={true}`, `text-base font-normal text-slate-600 mt-2` caption class. |
| `src/viz/DonutChart.tsx` | Thin `notMerge` wrapper with caption prop | VERIFIED | File exists, structurally identical to `TierShareChart.tsx`, same caption class. |
| `src/state/__tests__/selectors.test.ts` | VIZ-07 test blocks: 6-series, D-09 order, donut 5-items + borderColor, SHARE_CAPTION byte-equality | VERIFIED | 20 new VIZ-07 tests added across 3 `describe` blocks. All 72 selectors tests pass. |
| `docs/NEUTRALITY-STYLE-GUIDE.md` | Section 7 — D-16 rule with verbatim caption template | VERIFIED | Section 7 appended (line 172). Contains `Seeded:`, `Phase 5 gate:`, `### Context`, `### Required Caption Template (D-16)`, `### Constraints`, `### Neutrality Test`. Caption blockquote byte-identical to `SHARE_CAPTION`. Sections 1-6 unchanged. |
| `src/ui/AppShell.tsx` | `bandShares`/`shareOption`/`donutOption` useMemo chain + two `<section>` blocks | VERIFIED | Three new `useMemo` hooks (lines 82–93). Both `<section>` blocks present after Chart 3 (`relOption`) and before `<CitationFooter>`. D-12: `deriveBandShares(rawResult.series)`, not reinflated `result`. D-11 note rendered AppShell-side in both sections. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/core/tierBands.ts` | `src/core/distribution.ts` | `calibrateCurve` + `cumulativeShareFromTop` import | VERIFIED | Line 51: `import { calibrateCurve, cumulativeShareFromTop } from './distribution.js'` |
| `src/state/selectors.ts` | `src/core/tierBands.ts` | `BandShare` type import + consumed in `selectShareOption`/`selectDonutOption` | VERIFIED | Line 8: `import type { BandShare } from '../core/tierBands.js'`. Used as parameter type in both functions. |
| `src/viz/TierShareChart.tsx` | `echarts-for-react` | `ReactECharts` with `notMerge` | VERIFIED | `notMerge={true}` present (line 19). |
| `src/ui/AppShell.tsx` | `src/core/tierBands.ts` | `deriveBandShares(rawResult.series)` in `useMemo` | VERIFIED | Line 83: `deriveBandShares(rawResult.series)` |
| `src/ui/AppShell.tsx` | `src/state/selectors.ts` | `selectShareOption` / `selectDonutOption` / `SHARE_CAPTION` import | VERIFIED | Lines 16–18 of AppShell imports. Used in useMemos (lines 87, 91) and JSX (lines 162, 175). |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `TierShareChart` | `option` (EChartsOption) | `selectShareOption(bandShares, rawResult)` via `useMemo` | Yes — `bandShares` derived from `rawResult.series` (live engine output); `rawResult` from `projectionEngine(deferred, params)` | FLOWING |
| `DonutChart` | `option` (EChartsOption) | `selectDonutOption(bandShares, rawResult)` via `useMemo` | Yes — same `bandShares` and `rawResult` chain | FLOWING |
| `bandShares` (AppShell) | `BandShare[]` | `deriveBandShares(rawResult.series)` | Yes — `rawResult.series` is live `YearSnapshot[]` from `projectionEngine` | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `tierBands` test suite (12 tests) | `npm test -- tierBands` | 12/12 passed, exit 0 | PASS |
| `selectors` test suite (72 tests) | `npm test -- selectors` | 72/72 passed, exit 0 | PASS |
| Full test suite (163 tests, 12 files) | `npm test` | 163/163 passed, exit 0 | PASS |
| TypeScript compilation | `npx tsc --noEmit` | Exit 0, no errors | PASS |

---

### Probe Execution

No probe scripts declared or present in `scripts/` for this phase. Step 7c: SKIPPED (no probe scripts).

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| VIZ-07 | Plans 01, 02, 03 | Tier share-of-economy view: 100%-stacked-area + end-of-horizon donut, existing palette, mandatory neutral caption | SATISFIED | All five success criteria verified. Engine (`tierBands.ts`), selectors (`selectShareOption`, `selectDonutOption`, `SHARE_CAPTION`), wrappers (`TierShareChart`, `DonutChart`), AppShell composition, and `NEUTRALITY-STYLE-GUIDE.md` Section 7 all present and substantive. 163/163 tests green, tsc clean. |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/core/tierBands.ts` | 206 | Bare `catch {}` swallows all errors | WARNING | As noted by code review WR-04: any non-calibration error (e.g. a future bug in `computeBandFromCurve`) is silently substituted with plausible-looking-but-wrong bands. Does not break success criteria but creates diagnostic blindness. |
| `src/core/tierBands.ts` | 122–131 | `degenerateBand` emits uniform 0.2 split (valid-looking fabricated data) | WARNING | Code review WR-05: year-0 calibration failure renders as a real equal-distribution chart with no indication data is synthetic. Tied to CR-01 below. Does not break the non-fallback success criteria. |
| `src/state/selectors.ts` | 597–598 | Donut center label `hold ≥50%` derived from `topSetPercentile` (independent of rendered slices in fallback path) | WARNING | Code review CR-01: in the calibration-failure fallback, the center label asserts a quantitative claim inconsistent with the degenerate/carry-forward slices displayed. This is the most significant quality issue. In normal operation the claim is derived from the same model output as the rest of the donut; the inconsistency only arises in the edge-case fallback path. Does not prevent SC-3 from being met in normal operation. |
| `src/state/selectors.ts` | 579–583 | Per-slice `toFixed(2)` rounding discards D-01 exact-sum normalization | INFO | Code review WR-02: ECharts recomputes `{d}%` from independently-rounded values; displayed percentages may sum to 99.99/100.01. Minor numeric display issue; does not affect the underlying band computation. |

**No `TBD`/`FIXME`/`XXX` debt markers found** in phase-modified files.

---

### Human Verification Required

#### 1. Visual Composition of Charts 4 and 5

**Test:** Run `npm run dev`, open the app, observe the chart area below the existing three charts.
**Expected:** A stacked-area chart titled "Share of total wealth by tier over time" appears first, then a donut titled "Final-year wealth share by tier". Stacked-area bands always fill the full chart height at every year (no white gap at top, no overflow). A dashed teal line labelled "You" drifts across the bands. Donut shows 5 colored slices; the slice containing the user's final-year percentile has a visible dark (slate-800) outline border. Center text reads "Top X% hold ≥50% (year N)" with no exclamation mark.
**Why human:** ECharts stacking correctness, visual rendering of `areaStyle` accumulation, and the donut center label display cannot be verified by grep or static analysis.

#### 2. Real/Nominal Toggle Does Not Change Charts 4 and 5

**Test:** With the app running, enter a wealth value, observe Charts 4 and 5, then toggle the Real/Nominal switch.
**Expected:** Charts 4 and 5 do not change when the basis is toggled. Each chart section displays the note "Shares are identical in real and nominal terms — the real/nominal toggle does not affect this view." as visible text.
**Why human:** Requires observing live React state updates and DOM behavior on store change.

#### 3. D-16 Caption Visible as DOM Text

**Test:** Inspect the rendered page below each of Charts 4 and 5.
**Expected:** The text "This shows each tier's share of total projected wealth. Shares can shift over time while every tier's real wealth still grows — a falling share does not mean falling wealth, only that another tier is compounding faster. See the wealth-by-tier chart above for absolute amounts." is visible as static text below each chart, not inside a tooltip or hidden behind any user interaction.
**Why human:** Requires visual inspection of the rendered page to confirm DOM visibility and that it is not tooltip-only.

#### 4. Mobile Responsive: Sections Stack Without Horizontal Scroll

**Test:** Resize the browser to a narrow/mobile viewport (< 640px wide) or use device emulation.
**Expected:** The stacked-area section and donut section stack vertically. No horizontal overflow or scroll bar appears. All text and chart elements remain within the viewport.
**Why human:** Requires browser resize or device emulation to verify responsive layout behavior.

---

### Code Review Findings Assessment

The code review (04.1-REVIEW.md) identified 1 Critical and 5 Warnings. Assessment against success criteria:

**CR-01 (center label contradiction in fallback path):** Does NOT block any success criterion. SC-3 ("An end-of-horizon donut summarizes the final-year share split") is met in normal operation. The contradiction only arises in the calibration-failure fallback edge case (evolved distributions where alpha ≤ 1 at the final year). Classified as WARNING for correctness and neutrality quality; suggested fix is adding a `degraded` flag to `BandShare` and suppressing the quantitative claim when set. Not a gate blocker.

**WR-01 through WR-05:** All are quality improvement items. None block the stated success criteria.

---

### Gaps Summary

No gaps. All five roadmap success criteria are met by substantive, wired, data-flowing implementations. The code review findings are advisory quality items, not success-criteria failures. Human visual verification is required before marking `passed`.

---

_Verified: 2026-05-17T17:40:00Z_
_Verifier: Claude (gsd-verifier)_
